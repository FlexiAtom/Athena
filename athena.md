---
id: athena
title: Athena —— 面向 AI Agent 的工作协议（Working Protocol）
kind: proposal
status: pool
created: 2026-09-19
updated: 2026-09-19
priority: P1
tags: [tooling, cli, ai-collaboration, fidus]
---

# 提案 · Athena

> **状态**：🔵 池中（`~/.Athena/projects/athena/pool/`）· kind: proposal
> **平台**：Linux 独占 · **语言**：Rust 单二进制
> **一句话**：Athena 是一份**工作协议**——它定义 AI Agent 的职责、流程、禁忌与记忆位置，让"走流程"成为推进状态的必要条件；`athena` CLI 只是协议的执行边界，目录即状态，文档即记忆。
>
> 不叫"角色卡"：角色卡是完整人格设定（身份、语气、世界观、扮演一致性），Athena 只取其中"纪律人格"这一小部分，二者有底层区别。详见 §1.3。
>
> **两条正交的轴**（§1）：文档类型（提案/草案/方案 = 它是什么）× 推进状态（池/进行中/结束·冻结/社区互助 = 推到哪一步）。**目录只管后者，文档自己管前者。**

---

## 0. 认知前提：状态不是标签，是文件

这是本提案与所有"工作流引擎"方案的分水岭。

在 fidus 中，一个工作的状态不是 `status: draft` 这样的字段，而是：

- 它**在哪个目录里**（`docs/proposals/` vs `docs/drafts/`）
- 它**写了什么**（有没有"自审裁枝"章节、有没有可执行方案）

**状态 = 位置 + 内容**。改状态就是移动文件 + 修改内容，不是改一个枚举值。

对 Athena 的影响是根本性的：

| 传统状态机 | Athena |
|---|---|
| 状态是枚举值 | 状态是目录路径 |
| 转换 = 修改字段 | 转换 = `mv` 文件 + 追加剪枝记录 |
| 校验 = 检查字段合法性 | 校验 = 检查文件在不在对的位置、有没有伴随证据 |
| 类型系统封死转换 | **文件系统结构封死旁路**（没有 CLI 命令能跳过剪枝直接挪文件） |
| 需要数据库/索引 | 目录树本身就是索引，`ls` 即查询 |

因此：**Athena 不需要实现状态机，它只需要保护一个目录结构。** 状态机是 `~/.Athena` 的骨架，CLI 是移动文件的工具。

**而"状态"这个词本身要拆成两条正交的轴**（详见 §1 目录树处的定义）：

| 轴 | 回答什么 | 由什么表达 | 取值 |
|---|---|---|---|
| **文档类型** | 它是什么 | 文档内容 + frontmatter `kind` | 提案 / 草案 / 方案 |
| **推进状态** | 推到哪一步 | **目录位置** | 池 / 进行中 / 结束·冻结 / 社区互助 |

- **提案** = 我有个想法；**草案** = 我打个草稿，这东西怎么实现；**方案** = 我可以按照这个方案实施
- **池** = 未推进的提案；**进行中** = 我正在推进的提案/草案/方案；**结束/冻结** = 已完成 / 已毙；**社区互助** = 字面意思，放出去请人帮忙处理

> 旧版把这两条轴混在一起（表现为 `working/draft/`、`working/plan/` 嵌套目录），导致"文档是什么"与"推到哪一步"纠缠。现在：**目录只管推进状态，文档自己管类型。**

---

## 1. 家：`~/.Athena` 是一份工作协议存档（状态库即 git 仓库）

```
~/.Athena/                        ← git 仓库根（本地，永不推送到项目）
├── .git/
├── config.toml                   # 默认项目、行为开关
├── templates/                    # 【可编辑】覆盖内置模板
│   ├── proposal.md.tpl            # 提案模板（内含剪枝章节 + 决策日志，§13.1）
│   ├── plan.md.tpl                # 方案模板（同上）
│   └── terms.local.toml
├── terms.md                      # 【可编辑】术语与转换规则，AI 读它
├── pitfalls/
│   └── global.md                 # 全局被坑文件（离谱的、跨项目的）
├── .locks/                       # 并发锁（§13.2，按需生成）
│   ├── global-pitfalls.lock
│   └── <project>/<slug>.lock
└── projects/
    └── <name>/
        ├── meta.md               # 项目是什么
        ├── pitfalls.md           # 项目级被坑文件
        ├── pool/                 # 池：未推进的提案
        ├── working/              # 进行中：我正在推进的提案/草案/方案
        ├── finished/             # 结束：已完成（含毙掉的，用 frontmatter 区分）
        └── community/            # 社区互助：拿出来请人帮忙处理的
```

#### ⭐ 两套正交概念：文档类型 × 推进状态

> **这是本提案最容易被搞混的地方，必须先钉死。** 此前版本把"提案/草案/方案"和"池/进行中/…"混在同一条轴上（表现为 `working/draft/`、`working/plan/` 这种嵌套目录），导致"文档是什么"和"推到哪一步"两件事纠缠不清。现按 fidus 的原始语义拆成**两条正交的轴**：

**轴一 · 文档类型（内容层级，写在文档自己身上）**

| 词 | 定义 | 说明 |
|---|---|---|
| **提案** | 我有个想法 | 只要说清动机、目标、约束、为什么值得做 |
| **草案** | 我打个草稿，这东西怎么实现 | 回答"怎么做"——技术路径、模块划分、关键决策 |
| **方案** | 我可以按照这个方案实施 | 回答"照着做就能成"——可执行到步骤级、有验收标准 |

> 三者是**同一份文档的深化过程**：一个 slug 从提案写成草案、再写成方案，**文件不换目录**，内容在自己身上长。文档类型由 frontmatter 的 `kind` 字段 + 内容自述表达，**不靠目录区分**。

**轴二 · 推进状态（位置，由目录表达）**

| 词 | 定义 | 目录 |
|---|---|---|
| **池** | 未推进的提案 | `pool/` |
| **进行中** | 我正在推进的提案/草案/方案 | `working/` |
| **结束** | 已经完成 | `finished/`（`status: done`） |
| **冻结** | 已经毙了 | `finished/`（`status: frozen`） |
| **社区互助** | 字面意思：放出去请社区帮忙处理 | `community/` |

> **关键简化**：
> - **不再有 `working/draft/` 与 `working/plan/` 的嵌套**——"进行中"本身就是位置，文档类型写在文件里
> - **不再有 `done/` / `frozen/` / `suspended/` / `archives/` 四个目录**——"完成"与"毙掉"都是"已结束"，只在 frontmatter 用一个 `status` 字段区分，**不靠目录拆两处**；归档即留在 `finished/`
> - **新增 `community/`**——字面意思的社区互助出口：拿出来请人帮忙审/做/否决的，供**人**搬运出去（贴群、发 issue），**不是机器自动同步**（与 §13.5 跨机不互通一致）

**两条轴的组合示例**：

| 文件位置 | 含义 |
|---|---|
| `pool/foo.md`（`kind: proposal`） | 有个想法，还没开始推 |
| `working/foo.md`（`kind: proposal`） | 这个想法正在被推进 |
| `working/foo.md`（`kind: draft`） | 正在推进，且已写清"怎么实现" |
| `working/foo.md`（`kind: plan`） | 正在推进，且已可照着实施 |
| `finished/foo.md`（`status: done`） | 做完了 |
| `finished/foo.md`（`status: frozen`） | 毙了 |
| `community/foo.md` | 放出去请社区帮忙 |

> 一句话：**目录回答"推到哪一步"，文档自己回答"它是什么"。** 前者是位置（状态），后者是内容（类型）——两者正交，不要再用嵌套目录混在一起。

### 1.1 为什么在家而不是项目里

项目仓库里**零 Athena 状态文件**。这天然解决了"嵌入项目导致不得不推送"的痛点——物理上不可能 push 错。

#### 但项目里要有一个「协议入口文件」

**Athena 的全部状态都在 `~/.Athena/`，但每个需要使用 Athena 的项目仓库根目录，应当放置一个 AI 可见的协议入口文件**——它唯一的作用是 Agent 进入项目时把协议上下文注入进去：告诉 AI "存在一份叫 Athena 的工作协议、命令是什么、状态在哪边、你不许自己写文件。"

这个文件**不是给开发者读的说明文档，而是给 AI 读的协议入口**。所以它必须满足的唯一条件是：**AI 打开这个仓库时能自动读到它**。

**文件名不是固定的。** 只要目标 AI 工具能自动加载并把协议注入上下文，叫什么都行：

| 文件名 | 被谁自动读取 |
|---|---|
| `AGENTS.md` | Claude Code、Codex、通用约定 |
| `CLAUDE.md` | Claude（项目专属） |
| `.cursor/rules/*.mdc` | Cursor |
| `.github/copilot-instructions.md` | GitHub Copilot |
| `GEMINI.md` | Gemini Code Assist |

`athena init <project>` 默认复制为 `AGENTS.md`（覆盖最广），但**用户可以改名、复制多份、或软链到上述任一路径**——Athena 不感知也不关心这个文件的名字，它只负责生成内容。核心原则：

> **名字是手段，可见性是目的。** 叫 `AGENTS.md` 还是 `CLAUDE.md` 无所谓；关键是 AI 真的会读它。哪个工具是主力，就按那个工具的约定放。

**AI 直接读写 `~/.Athena/`，这是核心原理的必然推论，不是被"允许"的特例。**

> ⭐ **Athena = 提示词工程为原理 + AI 驱动**（§1.3）。既然是"AI 驱动"，那么 AI 就是状态机的**主体**——它必须能直接读写与演进状态库。若 AI 不能直接改，它就退化成提线木偶，每次变更都要人通过 CLI 代劳，那是"人驱动"不是"AI 驱动"。
>
> 因此这句话不能写成"Athena **允许** AI 直接读写"（那仍隐含着恩赐与防守姿态），正确表述是：**AI 天然拥有对 `~/.Athena/` 的读写权，这是它作为驱动者的前提。**

这与 fidus 的做法完全一致——fidus 的 AGENTS.md 就是让 AI 直接编辑项目里的提案/剪枝文档，Athena 只是把这份"可编辑的协议记忆"从项目仓库搬到了 `~/.Athena/`。

- AI 可直接编辑 `templates/`、`terms.md`、`terms.local.toml`、`pitfalls/`、`projects/<name>/` 下的协议与记忆文件（改模板、加术语、记坑、修订旧文档）
- 理由不只是"顺手"：**协议文本与记忆必须能被 AI 即时改写**（"术语即配置，改完即生效"），否则又会回到"流程演进锁死在发版周期里"的老路——那正是 fidus 要解决的问题
- **`athena write/append` 是推荐入口，不是唯一入口**：它顺手（canonicalize、路径白名单、自动 git commit），但 AI 用宿主工具的文件编辑能力直接改 `~/.Athena/` 内文件**同样合法**——只是该次改动不会自动 commit，需随后 `athena log --amend` 或下次写入时一并入库
- **唯一的位置红线**：状态内容**不离开 `~/.Athena/`、不进入项目仓库**——隔离的是**位置**，不是**权限**。这条由 CLI 防呆 + 协议共同维持，不是靠禁

> **CLI 是脚手架，不是镣铐。** 它存在的意义是让愿意做对的 AI **更容易做对**（顺手、留痕、自检、防呆），而不是让 AI 不得不。二者共同维持同一条线——**状态库内容可由 AI 自由演进，但绝不离开 `~/.Athena/`**。

**这个文件是无状态的。** 它只描述协议摘要（命令清单、状态根约定、门禁规则、不用的后果）——不含任何工作状态。因此：

- 放进仓库一起提交**完全安全**（它不暴露 `~/.Athena` 的内部内容）
- 别人 clone 下来，`athena` 命令不存在也无妨——只是一段提示
- 删掉它也不影响工作流（只是 AI 少了一个入口）
- **不得把 `~/.Athena/` 的绝对路径硬编码进这个文件**——否则一旦被误提交就泄露本地结构；内容应只引用"约定俗成的状态根"，路径由 `athena context` 运行时输出

#### 更新路径（重要：协议演进如何到达项目）

`AGENTS.md` 是**复制**进项目的（§「安装语义」），复制后就与 `~/.Athena/templates/AGENTS.md.tpl` **断开了引用关系**。因此 Athena 协议本身升级时，项目里的 `AGENTS.md` **不会自动更新**——这是设计（避免静默改动用户仓库），但需要明确的更新路径：

```bash
athena init --sync              # 仅更新已存在的协议入口文件：diff 三路合并，保留用户本地改动
athena init --sync --dry-run    # 预览差异，不写入
athena init --sync --theirs     # 完全采用模板版本（丢弃本地改动，慎用）
```

**更新策略**：

1. **默认不更新**：`athena` 命令升级后，不主动触碰项目里的 `AGENTS.md`，避免意外改动用户仓库
2. **显式同步**：`athena init --sync`（在项目根目录执行）比对 `~/.Athena/templates/AGENTS.md.tpl` 与项目现有文件，输出 diff 并三路合并
3. **版本锚点**：项目 `AGENTS.md` 的 frontmatter 记录 `athena-version: <semver>`，`--sync` 据此判断是否需要更新及是否可自动合并
4. **冲突处理**：模板与本地都有改动时，标记冲突段 `<<<<<<< template / ======= / >>>>>>> local`，由用户手动解决（不静默覆盖）
5. **新项目不受影响**：`athena init <new-project>` 始终用当前模板最新版

> **语义对照**：`~/.Athena/templates/` 是协议的"上游"，项目 `AGENTS.md` 是"检出副本"。上游演进 → 副本需显式同步，这与"接口可以进仓库、状态绝不进"（§「物理隔离」）一致——**接口文件允许在用户仓库内被版本化与本地化**，因为它无状态、可安全提交。
>
> 注意：`--sync` 只更新协议入口文件本身，**不**更新项目内的业务代码或状态（状态本就不在项目里）。若协议演进引入了新命令/新字段，需用户自行评估是否回填既有剪枝记录。

#### 膨胀控制与事后审计（协议入口文件会长大，必须管）

协议入口文件是**常驻上下文**（Agent 每次进入项目都读），因此它有一个别的文档没有的硬约束：**越大越失效**——内容膨胀会稀释指令预算、被模型忽略或截断（Red Hat 等实践建议控制在 ~150 行内）。而它又允许被 AI/人就地编辑（§1.1），所以膨胀与漂移是**必然发生**的，必须主动管：

**① 检查（防膨胀于未然）**：

```bash
athena agents check                    # 报告行数 / token 估算 / 各章节占比
athena validate                        # 超阈值 → 标红警告（默认 warn，可配 mode=block）
```

- 内置预算：默认 `max_lines = 150`、`max_tokens ≈ 2000`（可在 `terms.local.toml` 调）
- 超阈值时 `validate` 输出**哪几段最占地方**，提示"该精简或外置"
- **外置策略**（膨胀时的正解）：把细节移出常驻段——命令速查表保留，但"不用的后果""完整规则"改为**按需**由 `athena context <section>` 拉取，入口文件只留索引与最短协议摘要

**② 事后审计（改了什么、谁改的、何时改的）**：

入口文件在用户仓库里，可能被 AI 或人就地处改（不像 `~/.Athena` 有 CLI 自动 commit）。因此审计分两处：

- **项目侧**：入口文件本身随用户仓库 git 版本化 → `git log -p AGENTS.md` 可查改动历史（这是把它放进仓库的**额外收益**）
- **Athena 侧**：`athena init --sync --dry-run` 输出与上游模板的 diff；`frontmatter.athena-version` 记录来源版本，据此判断"落后多少/漂移多少"

```bash
athena agents diff                     # 与 ~/.Athena/templates/AGENTS.md.tpl 的差异
athena agents history                  # 本项目入口文件的改动轨迹（基于 git + 事件日志）
```

**③ 漂移的正确处置**：入口文件若被本地改得与上游差异过大，`--sync` 会标记冲突段由人手动解决，**不静默覆盖**（§「更新策略」第 4 条）。若确认本地改动更优 → 反向提交回 `~/.Athena/templates/`（人驱动的上游回流，不是自动同步）。

> 一句话：**入口文件是常驻上下文，必须短；它可编辑，因此必须能查是谁把它改长的。**

**`athena init` 的完整语义**：

```bash
athena init <project> [--agents-file AGENTS.md]   # 新项目：实例化 + 复制入口文件
athena init --sync [--dry-run|--theirs]           # 已初始化项目：同步入口文件到最新模板
```

#### ⚠️ 协议加载是「假设」，不是「保证」——但一句开场白即可消除

**Sys 是 System Prompt 的简写**（下文均如此使用），指宿主工具会话启动时注入的最靠前的系统提示片段（如 Claude Code 的 `--append-system-prompt` / 全局配置、Cursor 的 "Rules for AI" 全局规则、Codex/Copilot/Aider 各自的系统提示或用户级规则入口）。**Sys 在"进入项目之前、任何文件被加载之前"就已存在，因此它比项目根的 `AGENTS.md` 更早、更稳、更优先。**

**整个协议生效的前提是：AI 读到了协议内容。这是一个可能不成立的假设，必须明确标注，不能默认它成立。** 而最常见的失效不是"读不懂"，而是"**读得太晚**"——`AGENTS.md` / `CLAUDE.md` / `.cursor/rules` 都依赖进入项目后才加载，且有多种失败路径：

- 工具可能不支持该文件名的自动加载
- 文件可能被 `.gitignore` 排除后未纳入上下文
- 上下文窗口被截断，协议摘要被挤掉
- 长会话中段后，开头注入的协议文本被稀释、模型"忘记"
- AI 读到了但**忽略**（提示词不是强制指令，模型可无视）

**fidus 的实证（本节被它修正）**：上面的失败路径确实存在，但**都不需要兜底**——因为 fidus 的做法是，在会话最开始**显式要求** AI 读入口文件：

> *“我们的任务是继续推进这个项目，请读取 AGENTS.md 后向我提问以校准认知，然后我们会正式开始工作。”*

**只需一次确定成功的加载。** AI 读完之后，协议内容会自然嵌入上下文全程有效——包括会话总结、折叠、续接。因此"AGENTS.md 会不会失效、中途会不会被忽略"，答案都是：**只要这一次读成功了，就不需要再担心**。中途稀释是真实存在的，但可被一句固定指令修复（重说一次开场白即可），而非结构性缺陷。

由此得到**使用契约**：进入项目后，先对 AI 说这句开场指令，再 `athena context` 拿项目状态，动手前 `athena validate`。这条契约只在 AI 愿意遵守时成立（§12.0）。

**因此 Athena 采用分层加载，把"协议摘要"尽可能往前放——但目的不是兜底，而是让那句开场白更容易被说出：**

| 档 | 位置 | 时机 | 内容 | 角色 |
|---|---|---|---|---|
| **第 0 档 · System Prompt（Sys）** | 宿主系统提示 / 全局规则 | **会话启动即注入，最靠前** | 极短摘要 + **那句开场白** | **提醒人说出开场白**（真正的作用是这个） |
| **第 1 档 · 用户/全局规则** | `~/.claude/`、`~/.cursor/` 等宿主用户级配置 | 进入项目前后均可 | 协议要点 + `athena` 命令索引 + 指向项目入口 | 备用提醒 |
| **第 2 档 · 项目入口** | `AGENTS.md` / `CLAUDE.md` / `.cursor/rules/*.mdc` | 进入项目后加载 | **只放项目相关薄层**：状态根路径、术语/坑表引用、protocol 版本、`athena-version`、详细规则按需拉取的指引 | **开场指令要求 AI 读的对象**（只需成功读一次） |
| **第 3 档 · 按需拉取** | `athena context <section>` 输出 | AI 主动调用 | 术语、pitfalls、某 slug 的剪枝章节、反证模板、validate 报告 | 加载后的按需补给 |

**关键设计（三点）：**

1. **Sys 的真实作用是"提醒人说出开场白"，不是自己把规则灌进去。** 用户应把那句开场白放进 Sys（`athena sys-export` 生成，见下），让它出现在最靠前的位置。**真正完成加载的是那句指令，不是 Sys 本身。** 只要 AI 按指令读完 AGENTS.md 一次，协议就随上下文续存到底——入口文件不需要时刻有效，也不必靠多层冗余兜底。

2. **AGENTS.md 是"项目接口"，不是"第一道锁"。** 它只放项目级薄层，不放完整规则，因此也不该承担"最优先被读"的职责——这正好解释了 §1.3「膨胀控制」的合理性：**入口文件越薄越好，详情全按需**。

3. **`athena context` 仍是 CLI 层的核心。** 开场指令里那句"读 AGENTS.md"负责完成一次性加载，context 负责之后按需补充细节。**开场指令负责"加载一次"，context 负责"加载什么"——分工明确。**

**缓解长会话稀释**：中途稀释是真实存在的，但它**可被一句固定指令修复**——重说一次开场白即可（成本极低，终身只需少数几次）。Sys 片段本身保持极短（≤N 行，见 `athena sys-export --max-lines`），只含门禁关键词与那句开场白；配合宿主的 `hooks`/定时提醒，在 promote、write 等关键动作前重新 `athena context` 与 `validate`。**纯文本约束的确定性拦截（如"未 validate 禁止 promote"）交给 hook，不交给提示词。**

#### `athena sys-export`：生成供用户塞进 Sys 的摘要

```bash
athena sys-export                       # 输出 ≤ 约定的 System Prompt 片段（默认格式）
athena sys-export --format claude       # Claude Code 系（--append-system-prompt 可直接用）
athena sys-export --format cursor       # Cursor Rules for AI
athena sys-export --format generic      # 通用 Markdown，适配 Codex/Copilot/Aider 等
athena sys-export --max-lines 40        # 控制长度，Sys 必须短
```

- 输出**核心是那句开场白**——"读 AGENTS.md 后向我提问以校准认知，然后正式开始工作"，附以极短摘要：身份 + 总纲（状态在 `~/.Athena`、项目零状态文件）+ 门禁 + "推进前先 `athena context` 再 `athena validate`"
- **不含**具体术语、pitfalls、命令全文——那些按需拉取
- 用户复制该片段到自己宿主的 Sys/全局规则即可；升级后用 `athena sys-export | diff -` 比对是否需要更新
- `athena agents check` 同时检查：Sys 片段是否过长、Sys 版本是否与当前协议版本一致

> **一句话：Sys 只负责"提醒人说出开场白"，那句开场白负责"让 AI 读一次 AGENTS.md"，AGENTS.md 负责"把协议带进上下文续存到底"。协议不需要被反复加载，它只需要成功加载一次。**

> 此假设的验证方式见 §12.3（反证实验 4）与 Phase 3 dogfood。若实测发现"AI 读到 Sys 但仍不遵守"才是主要失效模式，则迭代方向应转向**协议文本的措辞与结构**（§1.3 第一优先级），而非加强 CLI；同时用宿主 hook 把确定性拦截从"提示"升级为"动作前校验"。


#### 安装语义：`athena init` 复制，不软链

```bash
athena init <project> [--agents-file AGENTS.md]
# 1. 在 ~/.Athena/projects/<name>/ 实例化骨架
# 2. 复制内置 AGENTS.md.tpl → <project>/.AGENTS.md   （文件名可覆盖）
# 3. 不创建软链（避免暴露 ~/.Athena 绝对路径到项目树）
```

**复制而非软链**的理由：软链会把这个文件的来源路径写进项目里，一旦被误提交就泄露本地目录结构；而复制出来的 `AGENTS.md` 是纯接口（无状态、可安全提交），删了也不损工作流。这与 §1.1「物理隔离」的原则一致——**接口可以进仓库，状态绝不进**。

### 1.1b 用户与边界：只有一个市场

**目标用户只有一类**：由 AI 辅助开发、但无法信任其流程纪律的个人开发者。

不是"个人版 vs 团队版"两个产品，也不是"核心 A / 扩展 B"两个市场档。这个判断来自一次自我质疑——

最初想把用户拆成两档：**A** 是被 AI 坑过、渴求反证门禁的偏执单人；**B** 是偶发协作、要求仓库干净的轻量使用者。但这两个描述其实是**同一个人在不同时刻的状态**：

- 每个 B 都可能从 A 演化来——先被坑，才有纪律；也可以反过来，任何 A 迟早在某个项目上进入 B 模式（要分享、要导出接口）
- 反向陈述"**A 为什么不能是 B**"同样成立——因为二者共享同一套命令集、目录模型、门禁规则，差异只在**协作粒度这一个开关**

因此 A/B 不是两个市场，而是**同一用户在同一产品下的两种部署形态**。硬拆成两档是伪分法（详见 §11「用户分档」自身的反证）。

| 部署粒度 | 表现 | Athena 是否支持 |
|---|---|---|
| 纯个人 | 反证门禁、外置状态、仓库零文件 | ✅ 核心 |
| 偶发协作 | 协议入口文件、项目级/全局 pitfalls、导出上下文 | ✅ 同一产品的导出能力 |
| 团队强制共享 | PR 门禁、多人共写 proposal、统一 constitution | ❌ 不做 |

**关键**：用户不变，变的是是否打开"协作导出"这个开关。产品本质始终是——**一份强制 AI 走流程的工作协议 + 外置状态库，协作只是附加模式**。别人 clone 项目时，加载到的只是协议入口（无状态），不会因此被迫共享 `~/.Athena` 的记忆。

#### 非目标（明确不做，避免与既有方案正面对打）

- 团队级 spec 评审、PR 门禁、多人并发改 proposal → **OpenSpec / Spec Kit** 更合适
- 多角色文档接力（分析师/架构/QA）→ **BMAD** 更合适
- 简易 task board（backlog / in-progress / done）→ **AndSoOn** 等更轻
- **同一文档内的段落级实时协同**（两人同时改同一提案的不同章节）→ **CRDT / OT / 基于 Git 的实时协同工具**，Athena 只保证 **slug 级串行**（§13.1 推论）；需要更细粒度时不扩展 Athena，转介专业协作工具
- **记忆加密 / 访问控制（防读取）** → **明确不做**。技术上可以直接加密，但没必要——**守本身就是过度设计，因为 fidus 从不需要**（详见 §1.3「不设防」）

Athena 只做重这四件别人不做的事：**外置 `~/.Athena`、目录即状态、反证硬门禁、项目仓库零状态文件**。其余能力（团队治理、向量检索、IDE 集成）交由既有工具，或通过 `athena context` 输出兼容片段互操作。

> **并发与跨机的保证范围（§13）**：**单机内**——同一 slug **串行**（文件锁 + content-hash CAS），跨 slug **完全并行**，只读操作**不加锁**，代价是锁开销与偶尔被挡；**跨机**——**无持续同步关系**，新机器通过复制整个 `~/.Athena` 一次性传承，再经 `athena localize` 本地化清洗，之后两机完全独立（§13.5）。本地强一致 + 跨机快照传承，是两件事；**没有"多机一致性"这个概念**。

### 1.2 为什么是 git 仓库

`~/.Athena/` 是一份**工作协议存档**：它是协议（术语、模板、剪枝规则）与记忆（提案、剪枝记录、被坑、状态变迁）的共同载体，而 git 让这份存档具备**审计**能力，并作为**跨机传承的载体**（复制整个目录即含完整 `.git/` 历史，见 §13.5）。

每次 Athena 写操作自动 `git add -A && git commit`。获得：

- **免费的历史审计**：谁（AI/人）在什么时候把什么从哪挪到哪，git log 全有
- **免费的版本回溯**：剪枝记录被覆盖也能找回
- **免费的时间戳**：不需要自建日志表
- **bisect 可用**：状态出问题时能定位到具体转换

**git 是审计层**：任何改动（AI 就地编辑、CLI 写入、术语演进）都有 commit 记录，可 `athena log` 回溯、可 bisect 定位错误转换。

**git 是传承载体**：跨机传承 = 复制整个 `~/.Athena/`（含 `.git/`），一次即完整携带全部历史到新机器，再由 `athena localize` 本地化清洗（§13.5）。**git 在此只作为打包后的目录内容的一部分被整体复制，不承担"持续同步"职责。**

**git 不是回滚工具**：见 §1.3「文档即记忆，不回滚」——发现旧结论错误，就地修订并沉淀教训，不用 `git revert` 倒带工作流。

且因为它不在项目仓库里，**永远不会误推送**。

### 1.3 核心认知：Athena = 提示词工程为原理 + AI 驱动

> **⭐ 一句话定义（全提案的地基，其余一切都是它的推论）：**
>
> **Athena 是以提示词工程为原理、由 AI 驱动的状态机。**
>
> 两个关键词：**提示词工程**、**AI 驱动**。二者共同否决了"用机制约束 AI"这条路——下文所有设计都是这两句话的展开。

#### ① 提示词工程为原理 → 靠说服，不靠强制

Athena 的全部效力来自**协议文本被 AI 读到并理解**。它的作用机制是暗示、结构、示例、措辞——**让 AI 自己想做对**，而不是用机制让它不得不做对。

> 一旦改用"阻止/拦截/门禁"来维持流程，就不再是提示词工程，而是**用强制代替说服**。那会彻底偏离原理：因为强制只能约束"愿意被约束的执行路径"，而 AI 总有别的路径。

#### ② AI 驱动 → AI 是主体，不是被审查者

状态机的每一次转换、每一条剪枝、每一次记忆修订，**主体都是 AI**。人是方向提供者与决策确认者，不是执行者。

> 由此得到一个**不可违背**的推论：**AI 必须能直接读写 `~/.Athena/`**。
> 若 AI 不能直接改状态库，它就不是驱动者，只是提线木偶——每次变更都要人通过 CLI 代劳，那是"人驱动"不是"AI 驱动"。
> **因此：阻止 AI 直接改，与核心原理直接冲突。** 这不是"允许"（那仍隐含着恩赐），而是**原理的必然要求**。

**四层结构（据此重新定义职责）**：

| 层 | 是什么 | 职责（修正口径） |
|---|---|---|
| **工作协议**（Athena 本身） | 入口文件 + 术语 + 模板 + 剪枝/反证/成本对账流程 + 禁忌 | **让 AI 理解并自愿执行**——这是唯一的效力来源 |
| **状态库** `~/.Athena/` | 目录、文档、commit 历史 | 协议的记忆存档，**由 AI 直接读写与演进**（文档即记忆） |
| **CLI（Rust）** | `write / append / promote / validate / context` | **给 AI 的脚手架**：顺手、留痕、自检、防呆——**是辅助，不是镣铐** |
| **协议入口**（项目根规则文件） | `AGENTS.md` / `CLAUDE.md` / `.cursor/rules/*.mdc` / … | 把协议上下文注入 Agent |

**状态机是 AI 驱动的，不是代码驱动的。** 目录树定义了有哪些状态、怎么转换，但真正执行转换、判断"现在该不该剪枝、反证够不够硬"的是 AI——前提是它读到了协议。CLI 是**脚手架**（让愿意做对的 AI 更容易做对：顺手、留痕、自检、防呆，§12.0）；git 是**审计与传承层**（记录每一次变化、作为跨机复制快照的内容，§13.5）。二者都是支撑，都不是 Athena 本身。

> ⚠️ **不叫"角色卡"**。角色卡是完整人格设定——身份、语气、世界观、扮演一致性一应俱全。Athena 只取人格中"纪律"这一小部分：它定义 AI **应当怎样思考与推进**，不定义 AI **是个怎样的人**。抽象层有些相似（都有职责、记忆、上下文加载、边界），但语境不同会让这类词指向完全不同的东西，故一律不用。下文"协议"一词即 Athena 本身。术语对照见下方映射表。

> **口径修正（贯穿全文档）**：本文档早期版本把 CLI 写成"门禁/防线/唯一入口/权限网关"，把 AI 直编辑写成"被允许的特例"，把 `validate` 写成"拒绝操作"——**这些都是在错误框架（防守姿态）里打转**。正确口径见下表，全文已按此重写：

| 旧表述（防守姿态） | 修正后（提示词工程 + AI 驱动） |
|---|---|
| CLI 是门禁，AI 是被审查者 | CLI 是**脚手架**，AI 是**主体** |
| AI 直改 = 待封堵的漏洞 | AI 直改 = **核心特性的必然推论** |
| `validate` 拒绝 / 拦截 | `validate` 是**自检报告**：告诉 AI 当前状态，由 AI 自己决定 |
| `write/append` 是唯一入口 | 是**推荐入口**（顺手 + 留痕），直编辑**同样合法** |
| 锁防止并发损坏 | 锁是**避免 AI 自相冲突的提示**，报冲突而不强制 |
| 反证"硬门禁" | **强制留痕**（写出来即可），不阻塞推进 |

#### 协议入口加载（Agent 进入项目时注入上下文）

协议入口文件的作用不是"等工具去发现某个配置文件"，而是 Agent 进入项目时的一次**上下文加载**——把协议的职责、术语、状态根、门禁规则、禁写边界一次性注入。工程上借各工具对 `AGENTS.md` / `CLAUDE.md` / `.cursor/rules` 等的自动读取实现，但语义上它是**协议入口**，不是"被发现的文档"。命名可换（§1.1「命名不固定」），可见即加载；入口只放无状态指令，不放 `~/.Athena` 绝对路径、不放项目状态。

加载内容分级，避免一次性灌满上下文：

1. **常驻**：协议摘要——职责、禁忌、状态根约定、不用的后果
2. **按需**：任务开始时拉 `athena context`（池 / 草案 / 方案 / 术语 / pitfalls）
3. **临门**：promote 前再加载反证模板与剪枝字段要求

#### 新项目 = 新文件夹 + 复制主脉配置

`athena init <new-project>` 不从空骨架起步，而是从主脉（或最近初始化过的项目）**复制已验证的模板、术语、pitfalls** 作为初始快照，之后各自自由分化。全局 `pitfalls/global.md` 独立存在、`context` 时合并。这样既避免"全项目强制共享一份配置"的耦合，也避免从零重建的重复耗费。

#### 文档即记忆，不回滚

状态库里的文档就是记忆的当前内容，不是某次 commit 的快照。**发现旧结论错误，直接改正文并保留一行修订注**（如 `因反证 X 推翻原 Y，见 pitfalls#z`），不切回旧文件、不提供"回到某状态"的命令。

`~/.Athena` 仍用 git，但 git 的用途是**审计与跨机传承（作为复制快照的内容）**，不是回滚工作流。任何"想回滚"的诉求都说明记忆结构或流程有缺陷——**必须把诱因写进 pitfalls**：

- 反证不充分就 promote（如未实测平台能力便认定可行）
- 剪枝只提更优解、不做事后成本对账（沉没成本绑架）
- 术语/模板变更后旧文档未同步，导致 AI 按过期定义执行

记忆更新的唯一动作是**就地修订 + 沉淀教训**；倒带由人工基于历史信息完成，工具不代劳。

#### ⚠️ 修正：不回滚 ≠ 只增不减——必须允许 AI 压缩（防滚雪球）

> **"文档即记忆、不回滚"若被理解成"只许追加、不许收拢"，会滚雪球**：决策日志、多轮剪枝、反复的反证尝试不断堆积，文件从几百行涨到几千行。后果是**协议自身失效**——`context` 塞不下、AI 读不完只能看尾部、真正重要的结论被淹没在过程里。这与 §1.1「入口文件膨胀」是同一类问题：**记忆不是越全越好，是越能读越好**。

**因此明确：允许并鼓励 AI 压缩（compaction）。** 压缩不是回滚——它仍是**向前修订**，只是把已定型的历史折叠成摘要：

```bash
athena compact <slug> [--keep-last N]   # 压缩某工作项的历史内容
athena compact --project [--dry-run]    # 批量：先报告哪些文件超阈值，再逐项执行
```

**压缩原则（必须保留 vs 可以折叠）**：

| 保留（不可压缩） | 可折叠为摘要 |
|---|---|
| 当前决策与结论 | 已被取代的中间方案 |
| 失效边界（什么环境下不成立） | 反复失败的尝试过程（保留次数与最终原因） |
| **未清算的 `pending` 待测项** | 冗长的原始输出（保留结论 + 指向完整输出的引用） |
| 状态、slug、frontmatter 元信息 | 早期的草稿与自我否定 |
| 修订注（为何推翻旧结论） | 已被后续轮次覆盖的剪枝过程 |

**可追溯性不得丢失**（这是压缩与"毁尸灭迹"的分界）：

1. 压缩后保留一行摘要，并附**指向 git 历史的引用**（如 `历史见 commit 3f9a2c1`），需要时可 `git show` 取回完整内容
2. 压缩动作本身**必须留痕**：写一条事件（`compact: 折叠 N 段 → 摘要，原内容见 commit <hash>`）+ 决策日志一行
3. **`pending` 待测项不得被压缩掉**——它们尚未清算，压缩会让验证义务消失（§5.1f）

**触发时机**：

- `validate` 检测文件超阈值（默认 `max_lines`，可配）→ 标黄提示"建议压缩"
- 由**人发起**为主；AI 可建议，但**执行需人确认**（与 `localize` 一致：AI 出建议、人拍板，§13.5）

> 与 fidus 的一致性：fidus 用 `docs/history/`（被取代、勿据此实现）处理旧文档——那正是"压缩/归档"的朴素形态。Athena 把它做成显式命令 + 留痕 + 可回溯，而不是让历史无声堆积成噪声。
>
> 一句话：**记忆要能被收拢，但不能被抹掉。** 压缩折叠过程、保留结论与指针。

#### 迭代优先级（重要）

> **对协议的迭代，主要是改协议文本与模板，其次是改 CLI 的执行边界——不能颠倒。**

- **第一优先级：协议文本与模板**——职责怎么写、术语怎么定义、剪枝流程怎么措辞、反证模板怎么逼出真实输出。Athena 的质量几乎全在这里
- **第二优先级：CLI 的执行边界**——`write/append` 路径白名单、validate 硬规则、canonicalize 防越界。这些是"手与锁"，保证 AI 不绕过协议，但不定义协议本身
- **不应颠倒**：先设计好协议文本与模板（含反证、成本对账、术语驱动校验），再用 CLI 把边界封死。反过来——先做 CLI 功能再做协议——会得到"工具有了但 AI 不知道为什么要用"的空壳

这决定了整个项目的资源分配：**入口文件 + `templates/` + `terms.md` 是核心资产，Rust 代码是实现细节。** 换一门语言重写 CLI 不影响 Athena；改了协议文本的措辞却可能彻底改变 AI 的行为。

#### ⭐ 关键：复杂度对人的成本是 0

> 读到这里，Athena 看起来很重：术语表、模板、剪枝四问、反证实验、成本对账、并发锁、压缩、localize、清洗……**但这份复杂度是 AI 的，不是人的。** 这一条必须明说，否则会被"这也太麻烦了"的第一印象劝退。

**分工是这样的**：

| 谁 | 做什么 | 复杂度 |
|---|---|---|
| **人** | ① 冒出想法时说一句"记下来"；② AI 给出建议时**拍板**（保留/挂起/删除、采用/不采用）；③ 偶尔 `athena pitfall` 记一条 | **≈ 0** |
| **AI** | 写剪枝章节、填反证表、做成本对账、维护术语、执行压缩、跑 localize 分类、遵守门禁 | **全部在这边** |

**具体来说，人的操作面极其窄**：

- **进池**：人说"我想做个 X" → AI 跑 `athena new` + 写提案 → 人看一眼
- **推进**：AI 写剪枝章节 + 反证 → 人拍板"采用/不采用/挂起"
- **踩坑**：人说一句"这个坑记下来" → AI 跑 `athena pitfall` 分类并写入
- **跨机**：人复制目录 → AI 跑 `localize` 出处置建议 → **人逐项点头**
- **压缩**：AI 建议 → 人确认
- **术语 evolve**：AI 遇到新概念 → `athena term new` → 人确认命名

**人的全部动作可以概括为两句话：说想法、点头/摇头。**

**为什么这很重要**：Athena 的复杂度是**刻意压给 AI 的**——因为流程纪律本就该由执行者承担，而不是让使用者记住一堆规则。人的记忆是不可靠的、注意力是稀缺的；**让 AI 承担流程的重量、让人只做判断**，这正是 Athena 存在的理由（否则直接用 OpenSpec 让所有人写 schema 就好了）。

> 一句话：**Athena 是一台"把流程复杂度转嫁给 AI"的机器。人只负责想法与决策，其余全甩给 AI——所以对人是 0。**
>
> 反过来也解释了 §1.1 为何必须允许 AI 直接编辑 `~/.Athena`：**若 AI 没有写权限，这份复杂度就压不到它身上，会重新滑回人这边。** 允许 AI 动手，是"复杂度归 AI"的前提。

#### ⭐ 不设防：加密与访问控制都不做

> 自然会问：记忆这么重要，要不要加密 `~/.Athena`、做访问控制、防止别的工具/人读取？
> **不做。** 这不是"以后再说"，是明确的设计决定——**因为 fidus 从不需要**。

**理由**：

1. **fidus 从不靠保密**——它的工作流完全公开（文档就在项目里、可以被推送、谁都能看），照样生效。Athena 的效力来自"AI 愿意遵守"（§12.0），不来自"别人看不到"。**保密对纪律没有任何增益。**
2. **加密会直接破坏三处根基**：① §1.1 允许 AI 直接读写（密文它读不了也改不了，复杂度立刻滑回人身上）；② 纯文本可检索、可 `git show`、可 diff（审计与考古全废）；③ 跨机传承（§13.5）复制目录即用的便利消失。
3. **与「复杂度对人 = 0」直接冲突**——人会多出管密钥、挂载、解密的成本，而这份成本换不来任何纪律收益。
4. **"守"本身就是过度设计**。Athena 里所有看起来像"守"的东西，实际都不是阻止：

| 表面上的"守" | 实际是什么 |
|---|---|
| 反证要求（§5.1e） | 留痕 + 标红，**不阻塞 promote** |
| 解析校验（§9.1） | 报错交人修，**绝不自动改文件** |
| CLI 边界（§12.0） | 只对愿意走 CLI 的 AI 有效，**不阻止绕过** |
| 并发锁（§13） | 预案非必须，且**只在愿意走 CLI 时生效** |
| 责任边界（§13.8） | 人要求跳过就照做，**只留痕** |
| 防读取（本节） | **根本不做**——fidus 从不需要 |

> **贯穿原则：Athena 不阻止，只记录与提示。** 它是一台"把流程复杂度转嫁给 AI"的机器，**不是保险箱、不是权限系统、不是防火墙**。
>
> 今后任何试图让它"防住什么"的提议（加密、访问控制、强制门禁、hook 硬拦、禁止直接编辑），都应先问一句：**fidus 需要过吗？** 答案基本都是"没有"——那就别做。

#### 与相邻概念的最小术语映射

| 易误读的说法（弃用） | 本协议用语 |
|---|---|
| 角色卡 | 工作协议（仅取纪律人格部分） |
| 人设 / 人格 | AI 职责定义 |
| 世界观 / 上下文加载（旧称"世界树加载"） | 协议入口加载（入口注入上下文） |
| 记忆库 | 状态库 / 协议存档（`~/.Athena/`） |
| 记忆回滚 | 不适用——改为**文档修订 + 教训沉淀** |
| 加载入口 | 协议入口文件（Agent 进入项目时注入上下文） |
| CLI / 二进制 | 协议的执行边界 |

---

## 2. 三个关键点

### 2.1 内置模板（编译进二进制）

`include_str!` 打包模板，零外部依赖：

- `proposal.md.tpl` — 提案：frontmatter（slug/status/updated-by/updated-at/content-hash）+ 目标/动机/约束 + **内联剪枝章节**（自审裁枝 / 反证实验 / 更优雅 / 脑洞 / 成本对账 / 决策）+ `## 决策日志`
- ~~`prune.md.tpl`（**作废，§13.1**）~~——剪枝记录不再独立成文件，改为内联章节，由 `proposal.md.tpl` / `plan.md.tpl` 内含
- `plan.md.tpl` — 方案：可执行路径 / 状态对比 / DoD（同样内联剪枝章节 + 决策日志）
- `falsification.md.tpl` — **反证实验模板片段**（见 §5.1d，可复用，内嵌于提案的 `## 反证实验` 章节）：假设 / 命令或代码 / 真实输出 / 判定 / 失效边界
- **`AGENTS.md.tpl`** — **协议入口文件模板**（见 §1.1、§1.3）：协议摘要 / 命令清单 / 状态根 / 门禁规则 / 不用的后果。**`athena init` 会复制它到项目根目录**，文件名可通过 `--agents-file` 覆盖（详见 §1.1「命名不固定」）；任何可被 Agent 自动读取的规则文件（含主脉配置）都可充当协议入口，命名不固定，可见即加载

**优先级**：`~/.Athena/templates/` 下存在同名文件 → 用用户的；否则用内置的。改模板不需要改代码。

**统一文件骨架（一个 slug = 一个文件，剪枝记录内联）**：

```yaml
---
slug: <slug>                  # 必须与文件名一致
kind: proposal                # 文档类型：proposal(提案) | draft(草案) | plan(方案)
                              #   ← 回答"它是什么"，由文档自己表达，不靠目录
status: pool                  # 推进状态（冗余快照，真值在目录）：
                              #   pool(池) | working(进行中) | finished(结束) | community(社区互助)
                              #   finished 时另用 outcome: done(完成) | frozen(毙了)
project: <name>
updated-by: <session-id>      # §13.2 第 3 层
updated-at: <ISO8601>
content-hash: sha256:<...>    # §13.2 第 2 层（CAS）
---
```

> **`status` 是目录的冗余快照，真值永远是目录位置。** 写它是为了方便 `grep`/解析，但当它与目录冲突时**以目录为准**（§9.1 会标红提示不一致，由 AI 自己修正）。这符合 §0「状态不是标签，是文件」——字段只是视图，不是真相。

```markdown
# <slug>

## 目标 / 动机 / 约束
...

## 1. 自审裁枝
- 在解决真正的问题吗：
- 与已有架构冲突吗：

## 2. 原理实机验证（pool→draft 必填）
### 正证
- 假设：
- 最小原型 / 命令：
- 真实输出：

### ⭐ 反证实验（必须主动证伪，不可省略）
| 假设 | 反证实验（具体命令/代码） | 预期若假设错 | 真实结果 |
|---|---|---|---|
|  |  |  |  |

#### 待测（`--skip-falsification` 生成，§5.1e）
- [pending] ...（具象理由）...

- 失效边界：<在什么环境下结论不成立>

## 3. 更优雅 / 脑洞
...

## 4. 成本对账
| 维度 | 当前路径 | 更激进/优雅方案 |
|---|---|---|
| 已投入（沉没） |  |  |
| 剩余成本     |  |  |
| 成功概率     |  |  |
| 终局质量     |  |  |
- rejected_alternative（若维持现状，必填具体理由）：

## 5. 决策
采用方案 B；理由见成本对账。

## 决策日志（§5.4 快速通道的追加落点）
- [YYYY-MM-DD HH:MM] quick: ... — <session-id>
```

> 缺少 `## 2` 的 `### 反证实验` 或任一「真实结果」为空，`athena validate` **标红警告并记入缺失项**（**现在检查的是文件内章节，不再 glob 独立 prune 文件**，§13.1）；`slug` 与文件名不一致、`status` 非法、`content-hash` 不自洽同样**在自检报告中显形**（§9.1）。
>
> **这是模板层面的强提示，不是拦截。** 模板的作用是**让 AI 一眼看到该填什么**——它靠结构提醒 AI，靠 `validate` 报告把缺项指出来，**最终补不补由 AI 自己决定**（提示词工程 + AI 驱动，§1.3）。

### 2.1b 术语即配置：随时新增的接口

**术语是被坑出来的，不是设计出来的。** fidus 的术语（`池`、`草案`、`剪枝`、`Gate`、`零信任`）全部来自踩坑后的命名收敛，Athena 也必然如此——你无法在 v0.1 就穷尽未来所有术语。因此术语必须能**随时增加、随时修改**，且增改本身是走流程的：

```
~/.Athena/
  terms.md                # 全局术语表（人类可读，AI 必读）
  terms.local.toml        # 结构化定义（机器可读，驱动校验与代码生成）
  templates/              # 同上，可覆盖
```

**`terms.md`（人读 + AI 读）**——自由散文，描述含义与边界：

```markdown
## 剪枝（prune）
每次状态转换前必须完成的一次自审裁枝 + 可行性验证 + 更优路径追问 + 成本对账。
来源：fidus 提案→草案阶段；2026-09 新增「成本对账」源于 fidus 换赛道教训。

## 反证实验（falsification）
原理实机验证的核心环节：主动尝试证伪自己的前提，记录失败的尝试与真实输出。
来源：fidus 换赛道教训——当初若做了反证，第一天就能发现「Niri 上平台 API
恒返回空/(0,0)，success 为真」，不必死磕数天。无反证的提案等于没验证。

## 池（pool）
所有新想法、新需求的唯一入口。不在池里的东西不存在。
```

**`terms.local.toml`（机读，驱动行为）**——这是"增加术语的接口"：

> ⚠️ **`terms.local.toml` / `terms.local.md` 应避免手动编辑。** 它由 `athena term new / edit / validate` 等子命令维护；手动改动虽可被热加载，但绕过了结构校验（`require_fields` 拼写、slug 唯一性、schema 版本），容易产出"文件能读、规则却失效"的静默错误。若要临时调参，优先走 `athena term edit <slug>`，让 CLI 负责合法性与 git commit。
>
> 例外：调试校验器本身、或新增尚无可对应命令的字段时，可直接编辑；但提交前务必 `athena term validate` 确认无结构错误。

```toml
[term.prune]
slug = "prune"
synonyms = ["自审", "裁枝", "pruning"]
require_fields = [
  "self_review",
  "empirical_verification",      # 原理实机验证（正证 + 反证）
  "falsification",               # ← 核心：反证实验章节
  "alternatives",
  "rejected_alternative",        # 第4条：成本对账的具象化
]
enforce_on = ["promote", "complete", "resume"]

[term.falsification]                       # ← 独立术语，可全局复用
slug = "falsification"
synonyms = ["反证", "证伪", "实证", "实机验证"]
is_enforced = true                         # 不可由 terms.local.toml 关闭
exempt_when = ["document_only"]            # 纯文档类提案 → 改用反例检索
escape_hatch = "skip_falsification"        # 需具象理由，留痕并标红

[term.pool]
slug = "pool"
is_entry = true

[term.gate]
slug = "gate"
origin = "fidus"
definition = "能力探测门：按能力而非平台身份决定可用性"
```

**新增术语的完整流程（走 Athena 自己）**：

```bash
athena term new gate --origin fidus
# 生成 terms.local.toml 的 [term.gate] 骨架 + terms.md 的章节模板
# 编辑 → athena validate 校验语法 → git commit
```

**术语如何被消费**（模块化设计的核心收益，见 §6b）：

| 消费方模块 | 术语驱动的行为 |
|---|---|
| `athena-context` | 把 `terms.md` 全文 + `synonyms` 渲染进 AI 上下文 |
| `athena-validate` | 读取 `require_fields`，校验剪枝记录必填项（**改术语即改校验规则，零代码改动**） |
| `athena-render` | 模板里 `{{ term "prune" }}` 引用术语，重命名自动同步 |
| 未来：代码生成 | `require_fields` 可生成严格 struct（类似 fidus 类型系统，按需启用） |

> **这就是 fidus 的"能用类型封死的绝不靠约定"的 Athena 版本**：校验规则不写死在 Rust 里，而写在可被 AI 和人类共同编辑的 `terms.local.toml` 里。改规则 = 改配置文件 = 走一遍剪枝流程。代码与规则解耦。

**热加载**：所有命令每次执行时读取 `terms.local.toml`（文件很小，无性能问题），不缓存到二进制。改完保存即生效，无需重新编译。

**`--skip-falsification=<具象理由>` 生成待测项**（详见 §5.1f）：当反证实验确实无法实机执行时，此标记不意味着豁免，而是在 `pending.md` 登记一条待清算项，并写入剪枝记录的 `## 反证实验 / ### 待测` 段；`complete` 前必须实际执行并填写真实结果。理由必须具象（"CI 无显示器"而非"不方便"），否则 `validate` 标红。

**版本与迁移**：`terms.local.toml` 含 `schema_version`，破坏性变更走 `athena migrate`，但日常增术语不加版本号——加字段是兼容的。

### 2.2 初始化（状态机即骨架）

`athena init <name>` 实例化一整套目录。初始化完，状态机就存在了——它不需要在代码里被建模，它就是磁盘上的那棵树。

### 2.3 特定目录（路径防呆）

所有命令的路径参数**相对 `~/.Athena` 解析**，`canonicalize` + `strip_prefix` **防呆**（不是拦截）：帮 AI 避免把状态写到 `../` 逃逸路径、绝对路径或符号链接穿透的地方。

> 措辞修正：这是**防呆**（帮 AI 别写错地方），不是"拒绝越界的权限网关"。唯一的位置红线是"状态不进项目仓库"，由防呆 + 协议共同维持。

---

## 3. 写入子命令：给 AI 的顺手工具

**AI 是状态库的主体，天然可以直接读写 `~/.Athena/`**（§1.1、§1.3——原理的必然推论，不是恩赐）。因此 `athena write/append` **不是"唯一入口"，而是"顺手的推荐入口"**：它一次性完成写入 + 留痕 + commit，比直接编辑更省事；但直接编辑**同样合法**，不视作违规。

```bash
athena write <rel-path> -c "内容"      # 创建/覆盖
athena write <rel-path> --stdin        # 从 stdin 读（长内容）
athena append <rel-path> -c "内容"     # 追加（坑/日志用）
athena rm <rel-path>                   # 谨慎，需显式确认
```

**作用（都是"让 AI 更省力"，不是"管住 AI"）**：

1. **位置防呆**：canonicalize + 白名单，帮 AI 别把状态写进项目仓库或别的项目
2. **自动留痕**：每次写自动 git commit，附 actor 标记——省得 AI 自己 commit
3. **结构化校验**：渲染模板、校验 frontmatter、`terms.local.toml` 热加载生效——**顺手告诉 AI "你这份写得全不全"**

**直接编辑的场景**（同样合法，只是少一次自动 commit）：

- 改 `templates/*.tpl`、`terms.md`、`pitfalls/*.md` 等协议记忆文件
- 此类改动不走 `write/append`，需在下次写入时一并 `git commit`，或 `athena log --amend` 补录

> 一句话：**`athena write` 的价值是"顺手"，不是"许可"。** AI 不走它也能改，只是少了留痕与提醒——损失的是 AI 自己（下次 `validate` 看不到这次变更的完整上下文），所以协议劝它走，而不是工具逼它走。

**禁止**：

- 写入 `~/.Athena/` 之外（项目仓库、系统目录、`~/.Athena/projects/<别的>/`）
- 跳过剪枝就想 `promote`（留痕可缺、但 `complete` 前须清算，§5.1e/5.1f）

> 项目仓库零文件 → 误推送不可能。这一条与"允许 AI 改 `~/.Athena/`"完全兼容：**可读写的是状态库，不是项目**。

---

## 4. 两级被坑文件

```
~/.Athena/pitfalls/global.md          # 全局：离谱的、跨项目通用
~/.Athena/projects/<name>/pitfalls.md # 项目级：这个项目的技术债与约定
```

```bash
athena pitfall "AI 声称完成但没跑测试" --global
athena pitfall "L10 对数螺旋与主频锁定自相矛盾"  # 默认项目级
```

`athena context` 输出时：项目级必带，全局默认带（一般不会太长）。这是"每条对应一次真实踩坑"的 fidus AGENTS.md 精神，泛化为两级。

---

## 5. 剪枝：转换的唯一门票

**剪枝不是可选的反思，是推进的必要条件。** 文件在 `pool/`（池）里想进 `working/`（进行中），必须伴随剪枝记录。

剪枝记录是**文档内的章节**（自审裁枝 / 反证实验 / 更优雅 / 成本对账 / 决策 / 决策日志），**一个工作项 = 一个文件**——历史随文档一路走完 `pool→working→finished` 自动留存，不单独拆文件（拆两文件会让并发下的原子性与锁粒度破产，详见 §13.1）。每轮追加带时间戳分隔，写在 `## 决策日志` 段。

> **注意与"文档类型"的关系**：从提案写到草案、再写到方案（轴一：内容长个儿），**是同一份文件在自己身上改 `kind` 字段 + 补章节**，不是移动目录。而"池 → 进行中"（轴二）**才是移动文件**。两者不要混淆：
> - 写深了（提案→草案→方案）→ **改内容，`kind` 字段跟着变，文件不动**
> - 推进了（池→进行中→结束）→ **`mv` 文件，剪枝章节是门票**

### 5.1 剪枝必须回答的四件事（照抄 fidus）

1. **自审裁枝**：是不是在解决真正的问题？跟已有架构冲突吗？
2. **⭐ 原理实机验证（pool→draft 时，核心门槛）**：**不是"理论上可行"，而是"在本机真实环境跑出证据"。**
3. **更优雅 / 脑洞方案追问**（每次都要）：
   - 有没有更简单的等价做法？
   - 有没有更激进的重构路径？
   - 当前是不是过度设计？
4. **⭐ 成本对账（本提案新增）**：若第 3 步发现了更激进或更优雅的方法，**必须量化比较它与当前路径的成本，再决定去留**：

   | 维度 | 当前路径 | 更激进/优雅方案 |
   |---|---|---|
   | 已投入（沉没） | 已有多少可用代码/验证 | 是否从零开始 |
   | 剩余成本 | 到完成还差多少 | 到完成差多少 |
   | 成功概率 | 当前卡点在哪 | 新方案的未知风险 |
   | 终局质量 | 做完能到哪 | 做完能到哪（是否本质更好） |

   **判定规则**：若更优雅方案的「剩余成本 ÷ 终局质量增益」低于当前路径，则**必须切换**——沉没成本不作为留任理由。
   若判定为「成本更高，维持现状」，剪枝记录中必须**显式写出**那条更优路径被拒绝的具体原因（代码可定位的一句），而不是一句"继续当前方案"。

   > **⚠️ 第 4 条的本质，必须说清楚：它不是要求算出精确数字，而是迫使 AI 把比较显式写出来。**
   >
   > 这些维度（已投入/剩余成本/成功概率/终局质量）**大多无法精确量化**——要求"算准"会让它立刻沦为编数字的形式主义。它的真正作用是**打破 AI 的默认惰性**：
   >
   > - 不做对账时，AI 的默认输出是「我们已接近成功，继续当前方案」——**一句免费的、两边都能说的话**（fidus 教训：Day1 用它拒绝换赛道，Day2 用它否定旧路径）
   > - 做了对账，AI 就被迫把那句话**展开成一张表 + 一句具象拒绝理由**，这个展开动作本身就是价值：
   >   - 它让"继续当前方案"从**修辞**变成**可审查的断言**（写了剩余成本，就可能被追问"你凭什么说只差这点"）
   >   - 它让**沉没成本无法隐式发挥作用**（必须单列"已投入"且明确"不作为留任理由"）
   >   - 它为**未来复盘留下依据**（三个月后回看，能看到当初是"算过"还是"没算"）
   >
   > 所以校验的落点是：**表填了没有、拒绝理由具象不具象**——而不是数字准不准。这也与 §5.1e「反证降级为留痕」同源：**Athena 不替 AI 判断对不对，只强制它把判断过程写出来。**

#### 5.1a 原理实机验证：硬性要求

> **这是提案转草案的唯一门票。** 没有实机证据，文件不能离开 `pool/`——无论论证多么严密、AI 多么确信。

**"原理可行"是不够的。** 提案阶段最常见的死法，是"技术上成立"在纸面上成立，但对真实环境做了错误假设。因此门槛是**实证**，分两层：

**第一层：正证（它真的能做出来吗？）**

必须产出一段**真实可运行的代码或命令**及其**真实输出**，证明核心假设成立：

- 最小可复现原型（spike / 一次性脚本 / 独立 binary）
- 输出贴在剪枝记录里（命令 + stdout，带机器/版本/日期）
- 证明的是**最关键、最可能被证伪的那一个假设**——不是最容易验证的边角

**第二层：反证 / 边界（它在哪会失效？）** —— **更关键，因为这一层是 fidus 血泪教训的来源**

必须**主动尝试证伪**自己的前提，记录失败的尝试：

- 列出"如果这个假设是错的，会观察到什么"
- 实际去测那个观察，并报告结果（即使结果是"假设成立"）
- 明确**边界条件**：什么环境下结论失效

**没有反证的提案，等于没验证。** 因为最危险的错误不是"做不到"，而是"做到一半才发现假设是空的"——而这一点只有主动去反证才会暴露。

**反证必须具体到"会观察到什么"，并真的去测。** 这是从 fidus 换赛道教训提炼的硬要求，见下节。

#### 5.1b 反证必须可被实验否定 · fidus 的教训

> **核心命题**：如果当初在 pool 阶段做了原理实机验证（含反证），一开始就能发现「在 Niri 上，靠平台 API 定位自己的窗口，只有空值和 `(0,0)`」——那根本不会有"死磕 Niri"这几天，**第一天就该写出 fidus**。

**反证应观察到的现象**（事后已知，但事前完全可以设计）：

| 假设 | 反证实验 | 预期（若假设错） | 真实结果 |
|---|---|---|---|
| Wayland 有窗口全局坐标 API | `find /usr/share/{wayland,wayland-protocols,wlr-protocols} -name '*.xml'` 逐个检查带 x/y 的参数 | 存在某个 event 回答"我在哪" | **42 份 XML，0 个** |
| xdg_toplevel 会给位置 | 抓 `xdg_toplevel.configure` 事件 | 事件含 x/y | **只有 width/height/states** |
| Qt 的 `geometry()` 可用 | 实机调用并打印 | 返回真实坐标 | **恒返回 `(0,0)`，success 为真** |
| X11 回退可用 | `xwininfo -root -tree` + `xdotool search` | 列出 Wayland 原生窗口 | **rootless XWayland，7 个辅助窗口** |
| GetImage(root) 可用 | 直连 X server 调用 | 返回根窗口画面 | **BadMatch（rootless）** |

**关键洞察**：这五条**全都是一行命令、几秒出结果**——成本极低，却能在一个下午内把"平台 API 这条路是否走得通"从信念变成事实。而它们**没有被在提案阶段执行**，是因为流程没有强制"必须实测反证"。

**因此 Athena 的规则**：`pool → working`（池→进行中）的剪枝记录**必须包含一个 `## 反证实验` 章节**，列出：

- 被验证的假设
- 具体命令 / 代码（可复现）
- 真实输出（或附件路径）
- **结论：该假设是否依然成立，以及失效边界在哪**

`athena validate` 对此做**结构化留痕**：文件内若无 `## 反证实验` 章节（含真实结果，或具象跳过理由 + `### 待测` 登记），则**标红警告并记录缺失项，但不阻塞推进**（§5.1e 已把硬门槛降级为留痕）。空泛的"已验证可行性"会在报告中显形——**由 AI 自己看到并补上，而不是被工具拦住。**

> 这正是 fidus 原则 1（"看起来合理的值"最危险）在流程层的落地：**你必须亲手制造出那个 `(0,0)`，并承认它就是全部真相——在那之前，提案只是猜想。** 而把"猜想"暴露出来的方式是**让它被写出来、被看见**，不是禁止它前进。

> 这正是 fidus 原则 1（"看起来合理的值"最危险）在流程层的落地：**你必须亲手制造出那个 `(0,0)`，并承认它就是全部真相——在那之前，提案只是猜想。**

#### 5.1c 历史教训：为什么"原理可行"必须被钉死在反证上

> **来源**：fidus 项目真实协作史——这正是本规则的由来。

**Day 1**：用户提出"换赛道"（当然不是只说两个字，而是对 Niri 死磕路径的根本性质疑）。
AI 回答：「xxxx……我们已经很接近成功」——用沉没成本辩护，把换赛道否决掉。

**Day 2**：用户拿出 fidus 原型（"投射自己的标记、截自己的屏、解自己的坐标系"）。
AI 回答：「xxxx……我们不应该和 Niri 死磕」——**用完全相同的措辞，把昨天的立场反过来**。

**问题诊断**：AI 在两条路上都说了同一套话——

- 走 A 时：「有更激进的 B，但成本太高，我们快成功了」→ 拒绝 B
- 走到 B 时：「整天不该在 A 上死磕」→ 否定 A

**"更优雅方案"因此沦为形式主义**：提出来只是为了表演"我思考过了"，从不真正改变决策。而真正该切换的时刻（Day 1 的原型方向），恰恰被"快成功了"挡了回去。

**更深层的问题**：Day 1 的对话**缺乏实机证据**。双方都在"讨论可能性"，没有人跑那五行命令。如果 Day 1 就做了反证实验：

- 第一条反证（42 份 XML 无坐标接口）就会**当场证明 A 路径的前提是空的**
- 不需要"换赛道"的争论，事实自己说话
- fidus 的诞生会提前好几天，**那几天的 Niri 死磕根本不会发生**

**这就是第 2 条被提升为核心门槛的原因**：原理实机验证（尤其反证）不是文档规范，而是**防止重复付出沉没成本的唯一机制**。一旦事实被跑出来，AI 就没有"快成功了"的修辞空间——它必须对着真实输出说话。

**第 4 条（成本对账）与此的关系**：第 2 条的反证提供了**可靠的输入**——当你知道 A 的前提已经失效（而不只是"感觉走不通"），成本对账才不是空谈。没有实机反证的成本对账，就是两个猜想在比谁听起来更合理。

> 对应两级被坑文件：
> - `原理可行 ≠ 原理已被实证；没跑反证实验的提案不离开池` → `pitfalls/global.md`
> - `在 Niri 上靠平台 API 定位自己：空值 + 恒返回 (0,0)，success 为真` → `pitfalls/global.md`（含完整反证命令，供未来项目复用）
>
> `athena context` 会把这两条渲染进 AI 的上下文，使"先实测、再讨论"在后续每次剪枝时强制可见。

#### 5.1d 对 fidus 反证命令的沉淀（术语级复用）

`pitfalls/global.md` 中固化的反证模板，本身就是一类**可复用的验证资产**——未来任何涉及"某平台是否提供某能力"的提案，都可以引用：

```markdown
## 反证模板：平台能力是否存在
- 假设：`<平台> 的 <API 族> 能提供 <能力>`
- 实验：枚举协议 XML / 抓真实事件 / 直连调用并打印
- 判定：若结果为空或恒返回 sentinel（0/0、None、success=true 但值非法），
  则假设被证伪，**不得继续该路径**，必须切换至不依赖该能力的方案
- 来源：fidus / Wayland 窗口全局坐标（2026）
```

这类模板随踩坑不断累积，成为项目级乃至全局的**验证语料库**——这正是 fidus 的"每条约定都对应一次真实踩坑"在 Athena 中的制度化形态。

#### 5.1e 反证实验降级为「留痕」（核心口径修正）

**反证实验不是"门槛"，而是"留痕"。** 它不再是 `promote` 的硬性前置条件，而是剪枝记录中**必须存在的一段结构化证据**——关键在于"被记下来、可被机器检查、可追溯"，而不是"必须由工具当场挡住"。

> ⚠️ 这是对原设计的修正：§5.1a/5.1b 把反证实验写成 `promote` 的硬门槛（`validate` 直接拒绝），§11 反证实验 2 第三条也暴露了它的失效边界（CI / 无显示服务器 / 纯文档提案）。现在把"强制挡住"降级为"**强制留痕**"。

**降级后的语义**：

| 原设计（门槛） | 新设计（留痕） |
|---|---|
| `## 反证实验` 缺失 → `validate` 拒绝 promote | `## 反证实验` 缺失 → `validate` **标红警告**，记录缺失项，但不阻塞 promote |
| 无真实输出 → 不可推进 | 无真实输出 → 必须填 `## 留痕`：`--skip-falsification=<具象理由>`，理由入库 |
| `--skip-falsification` 是逃生口 | `--skip-falsification` 是**正常路径之一**（见 §5.1f） |
| "不可关闭" | "**不可不留痕**"——关闭的是门禁，不是记录义务 |

**保留的硬约束只有一条**：`*.prune.md` 中**必须存在反证相关字段**（假设 / 实验 / 结果 / 失效边界，或具象的跳过理由）。空泛的"已验证可行性"仍不通过——但不是因为"没跑实验"，而是因为"**没有可被复核的证据记录**"。

**为什么降级是正确的**：

1. **反证的价值在"被写出来"，不在"被工具拦下"。** fidus 的教训之所以成立，是因为那五行命令**没有被写进提案**；一旦写出来，事实就自己说话，无需工具强制。
2. **CI / 离线 / 纯文档提案**确实无法实机反证，硬挡会产生"为了通过校验而伪造输出"的反向激励——比没有反证更糟。
3. **降级后仍可演进**：留痕字段足够结构化（机器可读），未来想恢复"硬门槛模式"，只需在 `terms.local.toml` 加一条 `require_fields` 或 `athena-rules` 加一个规则，**不用改协议文本**。

**留痕的结构化要求**（`terms.local.toml` 驱动，`athena-rules` 校验）：

```toml
[term.falsification]
slug = "falsification"
is_recorded = true          # 不可由配置关闭：反证必须留痕
fields = ["hypothesis", "experiment", "result", "failure_boundary"]
skip_field = "skip_reason" # 未实测时填 --skip-falsification 的具象理由
mode = "warn"              # "warn" = 标红但不阻塞；未来可项目级升级为 "block"
```

> 一句话：**Athena 不再替 AI 决定"够不够硬"，而是强制 AI 把"为什么这么判断"写下来。** 判断质量由协议文本（§1.3 迭代优先级）持续提升，而非由 CLI 一刀切。

#### 5.1f `--skip-falsification=<具象理由>` 生成一个「待测」

**跳过反证不是豁免，而是登记一条待办。** 当环境确实无法实测（CI、无硬件、纯文档推理）时，使用：

```bash
athena promote <slug> --skip-falsification="CI 环境无显示服务器，无法实机验证 Wayland 坐标 API"
```

`athena promote` 会：

1. 在剪枝记录的 `## 反证实验` 下自动写入：
   ```markdown
   ### 待测（pending verification）
   - 理由：CI 环境无显示服务器，无法实机验证 Wayland 坐标 API
   - 登记时间：2026-09-19
   - 状态：pending
   ```
2. 在该文件的 `## 反证实验` 章节下追加 `### 待测`（待清算），frontmatter 加 `falsification: pending`
3. 在 `~/.Athena/projects/<name>/pending.md`（或 `*.pending.md`）追加一条**待测项**：
   ```markdown
   - [ ] <slug> · <假设摘要> · 跳过理由：<具象理由> · 2026-09-19
   ```
4. `validate` 对 `pending` 项**标红提醒**，但不阻塞（除非项目级 `mode = "block"`）

**待测项必须被清算**：`athena complete <slug>`（plan → done）前，该 slug 的所有 `pending` 反证必须被实际执行并填写真实结果，否则 `complete` 拒绝（这是**唯一保留的硬阻塞**，因为"方案已完成却仍基于未验证假设"是不可接受的）。

> 设计意图：反证从"门槛"降级为"留痕"，但**不消灭验证义务**——只是把验证时机从"promote 瞬间"放宽到"complete 之前"。`--skip-falsification` 登记的是债务，不是免责。

---



### 5.2 转换规则（即"哪些移动合法"）

> 只有**推进状态（轴二）**才产生移动；**文档类型（轴一）**是原地深化，见下表区分。

**轴二 · 推进状态转换（移动文件）**

| 从 | 到 | 命令 | 门票 |
|---|---|---|---|
| `pool/`（池） | `working/`（进行中） | `promote` | 剪枝记录 + 反证留痕（§5.1e，标红但不阻塞） |
| `working/`（进行中） | `finished/`（结束，`outcome: done`） | `complete` | 剪枝记录 + DoD 核对 + 实测结果 + **`pending` 反证已清算**（§5.1f） |
| 任意 | `finished/`（结束，`outcome: frozen`） | `freeze` | 毙掉的原因（不能无声消失） |
| 任意 | `community/`（社区互助） | `community` | —（放出去请人帮忙，字面意思） |
| `community/` | `working/` | `promote` | 拿回来自行继续推进 |
| `finished/`（`frozen`） | `working/` | `resume` | 重新剪枝（不能无声复活） |

> **不再有 `suspended/` / `archives/` 独立目录**：挂起 = 留在原地不动（本来就不推）；归档 = 留在 `finished/`。**目录越少越不容易放错。**

**轴一 · 文档类型深化（原地改写，不移动）**

| 从 | 到 | 命令 | 说明 |
|---|---|---|---|
| 提案 | 草案 | `athena deepen <slug> --to draft` | 补上"怎么实现"：技术路径、模块划分、关键决策 |
| 草案 | 方案 | `athena deepen <slug> --to plan` | 补上"照着做就能成"：步骤级可执行 + 验收标准 |

> `deepen` 只改 `kind` 字段与内容，**文件留在原目录**。这是"写深了"，不是"推进一步"——与 `promote` 是两种完全不同的动作，此前混为一谈是主要的概念混乱来源。

#### 状态由「名称唯一性」识别，而非命令参数（§5.2a 修正）

**`<slug>` 在单个项目内全局唯一，且唯一标识一个工作项及其当前状态。** `promote <slug>` 不带"从哪到哪"参数，因为**状态由文件所在的目录决定**（§0：状态 = 位置 + 内容），命令只需负责"按当前位置推进一格"：

```bash
athena promote output-mutation
# 解析：在 ~/.Athena/projects/<name>/ 下查找名为 output-mutation 的工作项
#       → 找到它在 pool/（池），则推进到 working/（进行中）
#       → 找到它在 community/，则拿回 working/（继续推进）
#       → 找到它在 working/，则推进到 finished/（结束）
#       → 找到多个 / 找不到 → 报错并要求消歧
```

**为什么不让 `<slug>` 与状态共存（如 `promote <slug> --from draft --to plan`）**：

1. **语义不完整**：`promote` 的本意是"推进一格"，状态是目录位置的函数；显式传 from/to 等于让调用方自己算状态，既冗余又易不一致（"我说它在 draft，文件其实在 pool"）。
2. **避免共存歧义**：若同时支持位置推断与参数覆盖，两条信息冲突时以谁为准？去掉参数即消除一类错误。
3. **名称唯一性是天然的强约束**：slug 用作文件名、术语 key、git 引用，全局唯一能让 `athena log`、`athena context`、`pending.md` 都靠 slug 精确寻址，无需 ID 体系。

**唯一性规则**：

- **项目内**：同一 slug 在任意时刻只能存在于**一个**状态目录（`pool/` / `working/` / `finished/` / `community/`）
- **允许跨项目重名**：`<project>::<slug>` 是全局引用格式；不指定项目时默认当前项目
- **冲突处理**：`athena new <slug>` 若 slug 已在任意状态目录存在 → 报错 `SlugExists`，要求改名或先 `archive`
- **历史保留**：`finished/` 中的同名旧项不会阻止新建（已结束项不参与"进行中"唯一性检查，但 `athena new` 会提示"存在同名历史项，确认复用？"）

> 推论：`athena promote`、`athena complete`、`athena freeze`、`athena resume` **全部只需 `<slug>`**，不带状态参数。命令集因此更短、更不易误用，状态真相始终只有一处（目录树本身）。

### 5.3 AI 自动实现时的省略边界

> **省略人工确认，绝不省略剪枝与留痕。**

若明确为 AI 自动实现，`promote --ai` 可跳过人工确认环节，但剪枝记录、反证留痕、证据、actor 标记一样不能少；`--skip-falsification` 也必须填具象理由并生成待测项。AI 自己也要回答"有没有更优雅的做法"。

### 5.4 快速通道（Fast Path）：仅限小修复与配置更改

> **问题**：完整剪枝（自审 + 实机反证 + 更优雅方案追问 + 成本对账）是为"方向性决策"设计的。把它套在改一个错别字、调一个配置项上，是**荒谬的**——流程重量级会直接压垮小改动，最终结果是**人干脆绕过整个协议**。这比没有协议更糟。

**因此 Athena 提供快速通道**，让小修复与配置更改只走最小留痕。

```bash
athena quick <slug> -c "改了什么 / 为什么"        # 小修复/配置更改：一行留痕，不走完整剪枝
athena quick <slug> -c "..." --promote            # 顺带推进一格
```

**快速通道的留痕**（追加到该文件的 `## 决策日志` 段，不另建文件——保持"一个工作项一个文件"，见 §13.1）：

```markdown
- [2026-09-19 14:02] 修正 proposals.md 中术语拼写（"裁枝"→"剪枝"）
- [2026-09-19 15:30] 调 output-mutation 的超时配置 200ms→500ms，本地实测通过
```

只有**一行**：改了什么 + 为什么（或实测结果）。不需要自审、不需要反证、不需要成本对账。

#### 适用范围：明确的两种情形

快速通道**只开放给以下两类**，其余一律走完整剪枝：

**A. 小修复（trivial fix）**——不引入新行为、不改决策方向：

- 拼写、措辞、格式修正（文档、注释、日志文案）
- 补一行日志 / 修正日志级别
- 修正明显笔误、死链、过时引用
- 补一个遗漏的测试用例（不新增被测行为）
- 依赖的 patch / 小版本升级（无 API 破坏性变更）

**B. 配置更改（config change）**——只动配置项、不动代码逻辑：

- 调超时、重试次数、并发数、阈值等参数
- 增删配置字段（不涉及数据结构迁移）
- 切换 feature flag / 环境变量
- 调整构建、CI、lint 的配置文件
- 调整 `terms.local.toml` 的字段（但结构须合法，见 §5.5）

**不属于快速通道**（**必须走完整剪枝**）：

- 任何新提案从池进入进行中（`pool → working`）
- 换技术方案、引入新依赖、改数据结构 / 模块边界
- 改动推翻了已有的反证结论或失效边界
- 改动基于某个尚未验证的前提（"理论上可行"）
- 同一 slug 累计 quick 次数超过阈值（见下）

#### 防护：防止快速通道被滥用

> **最大的风险是 AI 把大改动也说成"小修复/配置更改"来逃课。** 这与 §5.1e「反证降级」面临的"形式主义"风险同源，因此防护同样靠**留痕 + 计数**，而非靠 AI 自觉。

1. **不得用于 `pool → working`**：新提案从池进入进行中**必须**走完整剪枝（含反证留痕）。快速通道只作用于已在 `working/` 或 `finished/` 的工作项
2. **累计计数强制升级**：同一 slug 累计 N 次（默认 N=5，可在 `terms.local.toml` 配 `quick_limit`）后，`athena quick` 拒绝并要求完整剪枝——防止"一直小改、实质是大改"
3. **留痕可审计**：记录照样进 git，`athena log` 可回溯；`validate` 按三条标准做关键字/结构提示（标黄而非阻塞）
4. **反证结论受影响时自动失效**：若触碰了已记录的反证相关文件，`validate` 标红并要求重新验证

#### 与「反证降级为留痕」的一致性

§5.1e 把反证从"门槛"降为"留痕"，§5.4 把小修复/配置更改从"完整剪枝"降为"一行留痕"——**二者是同一条原则的两次应用**：

> **Athena 不替 AI 决定"够不够"，而是强制 AI 把"做了什么、为什么"写下来，并按改动量级匹配记录重量。**

重量级匹配改动量级，而不是一律最重。这样协议才能长期存活——否则它会因为"太烦"而被整体放弃。

> 待验证：快速通道的滥用率。Phase 3 dogfood 统计 `quick` 与完整剪枝的比例；若滥用严重，则收紧标准或降低 `quick_limit`。

---

## 6. 命令集

```
初始化
  athena init <project> [--agents-file AGENTS.md]
                                   实例化 ~/.Athena 骨架；复制 AGENTS.md 接口到项目根

工作项
  athena new <slug>                  用提案模板在 pool/ 创建（项目内必须唯一）
  athena deepen <slug> --to draft|plan
                                    【轴一】写深一层：提案→草案→方案（改 kind + 内容，文件不动）
  athena promote <slug>              【轴二】推进一格（池→进行中→结束，不带 from/to）
  athena quick <slug> -c "..."       小修复/配置更改快速通道：一行留痕，不走完整剪枝
                                     [--promote 顺带推进；累计 N 次后强制完整剪枝]
  athena complete <slug>             进行中→结束（outcome: done，要求 pending 反证已清算）
  athena freeze <slug>               进行中→结束（outcome: frozen，毙掉需写原因）
  athena resume <slug>               已冻结→进行中（要求重新剪枝）
  athena community <slug>            放进 community/（社区互助：请人帮忙处理）

读写网关
  athena write <path> [-c|stdin]     写入
  athena append <path> [-c|stdin]    追加
  athena pitfall "..." [--global]    记坑（append 的专用形式）

术语（随时增加，走流程）
  athena term list                   列出所有术语
  athena term new <slug> [--origin]  新增术语骨架（生成 toml + md 模板）
  athena term show <slug>            显示定义与引用位置
  athena term validate               校验 terms.local.toml 语法

AI 接口
  athena context                     输出 AI 上下文（见 §7）
  athena validate                    目录/剪枝完整性检查（规则来自术语 + 反证实验强制）
  athena log                         git 历史
```

**`athena validate` 的核心规则**（其余校验由 `terms.local.toml` 驱动，可配置）：

0. **解析校验优先（§9.1）**：先校验涉及文件是否**可被工具解析**——`terms.local.toml` 语法、frontmatter 分隔线、标题层级、必需字段。**检出即报错并中止，精确报错位置，绝不静默容错、绝不自动改文件**（因为允许人直接编辑，文件可能处于半坏状态）。解析通过后，才进入下面对业务规则的校验。
1. **`pool/` 下任何文件，若文件内无 `## 反证实验` 章节 → 标红警告**（缺失即记录，不阻塞 promote；详见 §5.1e 降级说明）。
2. **该章节必须含具象的跳过理由或至少一条真实 `结果`**——空泛的"已验证可行性 / 理论上可行"会在自检报告中显形。项目级 `mode = "block"` 时可升级为拒绝 promote。
3. **`pending` 反证未清算 → `complete` 拒绝**（唯一保留的硬阻塞，§5.1f）。
4. **`status` 字段与所在目录不一致 → 标红提示**（真值是目录，字段只是视图，§1）。

第 1、2 条默认走"留痕 + 标红"而非拒绝；`[term.falsification].mode` 可在项目级设为 `"block"`。理由见 §5.1e（反证降级为留痕）与 §11 反证实验 2 的失效边界分析。

> 实现上：`athena-rules` 内置 `falsification_recorded.rs`，解析剪枝记录 Markdown，定位 `## 反证实验` 标题——检查"存在真实结果"**或**"存在 `--skip-falsification` 生成的 `### 待测` 段 + 具象理由"。前者缺失后者也无 → 报 `MissingFalsificationRecord`。校验对 `pool → working`（池→进行中）的转换**前置**执行（promote 先校验再移动），其余转换沿用 `terms.local.toml` 的 `require_fields`。**所有解析错误统一走 §9.1 的报错契约**。

**设计原则**：命令只是对特定目录的安全读写。没有"状态机层"，因为目录树就是状态机。

---

## 6b. 模块化代码设计：流程即装配

**目标**：便于调整流程。状态目录、剪枝规则、术语约束都能改，且改的是配置和数据，不是控制流。

### 分层（workspace crate）

```
athena/                          # 伞 crate，二进制入口
├── athena-core/                 # 与流程无关的基础件
│   ├── store.rs                 # Store：~/.Athena 的引用 + 路径解析 + 越界检查
│   ├── vcs.rs                   # GitBackend：auto-commit（可替换为 noop/远程）
│   ├── config.rs                # 加载 config.toml、模板解析
│   └── error.rs
│
├── athena-domain/               # 领域模型（纯数据，无 IO）
│   ├── term.rs                  # Term / TermsRegistry：解析 terms.local.toml
│   ├── document.rs              # Document、Frontmatter、PruneRecord
│   ├── workflow.rs              # Transition、Rule（校验规则的数据表示）
│   └── render.rs                # 模板渲染 trait（tera 或自研 mini）
│
├── athena-rules/                # 校验规则集（**可插拔**）
│   ├── lib.rs                   # Rule trait：fn check(ctx) -> Vec<Finding>
│   ├── required_fields.rs       # 读取术语的 require_fields 生成
│   ├── state_location.rs        # 文件位置 ↔ 状态合法性
│   ├── prune_complete.rs        # 转换必须有剪枝记录
│   └── registry.rs              # 内置规则注册；用户可在 projects/<name>/rules/ 加自定义
│
├── athena-context/              # 给 AI 的上下文装配
│   ├── builder.rs               # ContextBuilder：按需拼装各段
│   ├── sections/                # **每个段落一个模块，可增删**
│   │   ├── terms.rs             #   术语表（来自 terms.md）
│   │   ├── tree.rs              #   目录结构
│   │   ├── open_items.rs        #   进行中工作项
│   │   ├── pitfalls.rs          #   两级被坑
│   │   └── recent_transitions.rs#   git log 摘要
│   └── budget.rs                # token 预算分配（优先级 + 截断）
│
└── athena-cli/                  # 命令定义（薄：只做参数解析 → 调 domain/service）
    ├── commands/
    │   ├── init.rs
    │   ├── new.rs
    │   ├── promote.rs
    │   ├── term.rs              #   `athena term *` 子命令
    │   └── context.rs
    └── main.rs
```

### 为什么这样便于调整流程

**1. 规则是数据，不是 match 分支**

`athena-rules` 暴露 `Rule` trait。`required_fields` 规则在运行时读取 `terms.local.toml` 的 `require_fields`——**新增术语的必填字段，校验自动生效，不用改 Rust**。

```rust
pub trait Rule: Send + Sync {
    fn id(&self) -> &'static str;
    fn check(&self, ctx: &ValidationCtx) -> Vec<Finding>;
}
```

用户还能在 `projects/<name>/rules/my-rule.rs` 放自定义规则？→ 不，v1 不做动态加载（不安全 + 过度设计）。改为：**在 `athena-rules` 里加一个规则 = 加一个文件 + 注册一行**，重新编译即可。`terms.local.toml` 覆盖 90% 的"改规则"需求。

**2. context 是各段落的组装**

`ContextBuilder` 逐个 push section，每个 section 实现 `RenderSegment` trait。`--budget` 按优先级分配 token：术语与被坑文件永远保留，历史日志可截断。

想改 AI 看到的上下文结构？改 `sections/` 的装配顺序，或新增一个 section 模块。**不改调用方**。

**3. 模板渲染解耦**

`athena-domain/render.rs` 定义 trait，`athena-render-builtin` 提供默认实现（编译进二进制）。`~/.Athena/templates/` 覆盖时由 `athena-render-overlay` 处理。命令层不关心用的是哪个。

**4. 存储后端可替换**

`Store` trait 抽象路径解析，`GitBackend` 实现 auto-commit。本地 git 是默认，但测试用内存后端、未来想加密同步换一个实现即可。`athena-cli` 只依赖 trait。

**5. 依赖方向单一**

```
cli → domain ← rules
            ↑
        context → core → store/vcs
```

`domain` 不依赖 `cli` 与 `context`，可独立单测。规则不依赖 CLI。这与 fidus 的三层 trait 分层同构。

### 模块边界对照 fidus

| fidus | Athena |
|---|---|
| `fidus-core`（类型系统 + 三层 trait） | `athena-domain` + `athena-core` |
| `fidus-backend-*`（原语适配） | `athena-rules`（规则即可插拔后端） |
| `fidus-calibrate` / `fidus-estimate` | `athena-context`（装配管线） |
| Gate（能力探测） | `validate` + `terms.local.toml` 约束 |
| 坐标空间 `LogicalPoint`/`PhysicalPoint` | `Document` / `Transition`（流程的基本量） |

---

## 7. `athena context` — 给 AI 的完整上下文

```bash
athena context [--project <name>] [--budget 4000]
```

输出（**首段必须是协议摘要**，见 §1.1「协议加载是假设不是保证」——这是协议生效的关键加固）：

```
# Athena Context
# Project: fidus | Generated: 2026-09-19T10:30:00+08:00
# Truth: ~/.Athena/projects/fidus (git-tracked)

## 协议摘要（必读 · 不依赖入口文件）
你是 Athena 工作协议的执行者。
两条正交的轴，别搞混：
  ① 文档类型（它是什么）：提案=有个想法 / 草案=打草稿怎么实现 / 方案=可照着实施
  ② 推进状态（推到哪一步）：池 / 进行中 / 结束·冻结 / 社区互助 —— **由目录表达**
写深了 → deepen（改内容，文件不动）；推进一步 → promote（挪目录，剪枝是门票）。
禁忌：① 不进池就开始写代码；② 不剪枝就想 promote；③ 说"可行"必须贴真实输出；
     ④ 发现更优方案必须做成本对账；⑤ 不自行在 ~/.Athena 外写状态文件。
小改动走快速通道（`athena quick`，一行留痕）；大改动走完整剪枝。
违反协议 → validate 标红提示（自检报告，不是拦截）。不用的后果见协议入口文件。

## 进行中（working/）
### output-mutation [P0, blocked] · kind: plan（方案）
  目标：output 热插拔恢复
  阻塞：需真实 Niri 异常路径验证
  上次剪枝：2026-09-15 — 已排除轮询方案（更优雅：事件驱动；脑洞：让合成器主动告知）

## 池（pool/）
- windows-backend [P1] kind: proposal — 动机：fidus 缺 Windows 支持
- macos-backend [P2] kind: proposal

## 结束（finished/）
- manual-output-recovery [outcome: frozen] 原因：与 output-mutation 合并

## 社区互助（community/）
- xdg-protocol-question kind: proposal — 已放出去问社区

## 项目级被坑
- L10 对数螺旋与主频锁定自相矛盾，已否决

## 全局被坑
- AI 声称完成但没跑测试 → 必须实测结果才能 complete

## 术语与规则（来自 terms.md）
<本文档 §5 的转换规则被渲染进来>
```

**关键**：`terms.md` 被渲染进 context（协议加载时注入，§1.3）。改术语 = 改协议文本，**代码不动**。术语是协议的一部分，不是配置文件——它定义 AI 看到概念时如何理解与判断。修订术语采用"就地改正文 + 保留修订注"，不回滚（§1.3「文档即记忆」）。

---

## 8. 边界与差异化

### 8.1 与 OpenSpec 的关系：互补而非竞争

**一句话区分**：**OpenSpec 是「规范评审流程」，Athena 是「AI 纪律协议」。前者管文档质量，后者管 AI 的推进方式。二者甚至可能互补——Athena 的状态库完全可以在某个工作项需要正式评审时，导出给 OpenSpec 使用。**

| 维度 | OpenSpec（规范评审流程） | Athena（AI 纪律协议） |
|---|---|---|
| **管什么** | 提案文档的**质量与完整性** | AI 的**推进方式与思考纪律** |
| **门禁对象** | proposal / design / tasks 是否写全、schema 是否合法、archive 是否干净 | AI 是否先反证、是否做成本对账、是否留下可审计痕迹 |
| **典型问题** | "场景覆盖够不够？spec 合规吗？" | "你**真的测过**吗？有没有更优雅的方案、成本凭什么更低？" |
| **谁用** | 团队：多人评审、PR 门禁、CI 校验 | 个人 / 偶发协作：约束单个 Agent |
| **状态位置** | 项目仓库内（`openspec/`） | 外置 `~/.Athena/`，项目只有协议入口 |
| **价值观** | 规格即真相、变更可追溯、评审可审查 | 反想当然、偏执留痕、文档即记忆不回滚 |

**两者的不对称性**：OpenSpec **无法拦住**"AI 根本没做反证、却把 proposal 写得像模像样"——因为**文档格式合法性与事实可靠性是两个维度**。Athena 恰好补的是这个维度：它不关心 proposal 写得漂不漂亮，只关心 AI 有没有真实验证过自己的假设。

**互补关系（而非竞争）**：二者是上下游，不是替代：

```
Athena（前端纪律层）                  OpenSpec（后端评审层）
─────────────────────                ─────────────────────
个人对 AI 的强制纪律                  团队对文档的正式评审
池→草案→方案，反证留痕                 proposal → design → tasks → archive
快速、偏执、成本对账                   多人评审、CI 校验、可 PR
状态在 ~/.Athena（外置、不污染项目）    状态在项目仓库内（随代码 version）
```

Athena 状态库里的方案文档，在工作项**需要正式评审时**，可以通过 `athena export` 生成 OpenSpec 兼容的 proposal / design，进入 OpenSpec 流程。**Athena 把最脏最丑的反证和剪枝前置做完，OpenSpec 拿到的就是一份已经被 AI 纪律蹂躏过的干净提案**——它的评审通过率会高得多。

> **推论**：Athena 在这个维度上**没有竞争者，且是 OpenSpec 的上游**。现有工具都停在"制品层"（文档质量），没人管"AI 的行为层"（是否真实验证）。因此"生态空位"论据应这样表述——**不是技术无人会做，而是主流产品的默认价值排序把你这套放在非默认位置**（§11 反证实验 3 已验证）。

**边界（Athena 不做什么）**：多人必须共享同一份协议上下文、PR 门禁、并发写入、权限与审批流 → 交给 OpenSpec / Spec Kit / BMAD，Athena 只对个人 Agent 负责，偶发协作可只读协议入口，不强制共享记忆库。

### 8.2 与 fidus 的映射

| fidus | Athena |
|---|---|
| `docs/proposals/` | `pool/` |
| 提案→草案（自审裁枝） | `promote`（剪枝记录是门票） |
| `docs/drafts/` | `working/`（进行中） |
| 状态矩阵（README） | `athena context` 自动生成 |
| AGENTS.md 踩坑 | `pitfalls.md` 两级 |
| AGENTS.md 本身是 AI 接口 | **`AGENTS.md.tpl` 复制进项目根，作为协议入口文件，命名可覆盖，无状态** |
| `docs/history/`（被取代） | `finished/`（已结束） |
| 状态 = 文档在哪、写了什么 | **完全一致** |
| 工作流嵌在项目目录（被迫连同推送） | **工作协议外置 `~/.Athena/`；项目只有协议入口文件** |

---

## 9. 为什么这样更简单

之前那版（枚举 status + 类型系统封死转换 + 事件溯源）是**过度设计**。它把一个"移动文件"的问题，建模成了"状态机 + 事件日志 + 投影"的系统工程。

修正后：

- **无状态字段** — 位置即状态
- **无事件表** — git log 就是事件表
- **无类型强制** — 目录结构强制
- **无索引** — `ls` 即查询
- **无数据库** — 纯 Markdown + git

Athena 的全部复杂度，收敛到**"协议文本 + 初始化骨架 + 特定目录的安全读写"**三件事上——其中"协议文本"（入口文件 + 模板 + 术语）是核心资产，"安全读写"（CLI 执行边界）只是保证 AI 不绕过它。

### 9.1 解析校验：自检报告，操作前先把问题说清楚

> **前提**：AI 是状态机的主体，天然可直接编辑 `~/.Athena/` 下的协议与记忆文件（§1.1、§1.3，原理的必然推论）——改模板、加术语、修订旧文档都合法。**但这意味着文件可能处于工具无法解析的状态**：改了 `terms.local.toml` 的字段、删了 frontmatter 的分隔线、把 Markdown 标题层级弄乱、剪枝章节格式写偏。Athena 必须正视这种"半坏"状态，而不是假设文件永远合法。

**核心原则：操作前校验，把问题精确说出来，交给编辑者（通常是 AI）自己修。**

> ⚠️ **口径修正**：本节早期把它写成"报错交**人**修"——那仍是防守姿态（默认编辑者是人、AI 是破坏者）。正确表述是：**交给编辑者自己修，而编辑者通常是 AI**。校验的性质也因此变了：
>
> - 它不是"拦截错误操作的警察"
> - 它是**给 AI 的自检报告**：`athena validate` = "这是你当前状态库的完整度报告，缺什么我告诉你，你自己决定补不补"
>
> AI 随时可以主动跑 `validate` 做自我确认，这正是"提示词工程"期望的行为——**AI 主动想知道自己做得对不对**，而不是被工具逼着做对。

任何会**读取结构化内容**的命令（`promote` / `complete` / `validate` / `context` / `export` / 反证规则检查）在执行前，先对涉及的文件做解析校验；一旦检出问题，**立即中止并精确报告**，把修复决定权交给编辑者，绝不静默容错、绝不猜测意图、绝不自动"修正"（**自动修正等于替 AI 改记忆**，越权且危险）。

#### 校验对象

| 文件 / 内容 | 校验点 |
|---|---|
| `terms.local.toml` | TOML 语法、必需字段（`require_fields` / `quick_limit` 等）、字段类型、枚举取值 |
| 提案 / 草案 / 方案（frontmatter） | YAML 分隔线完整、`slug` 与文件名一致、`status` 是合法状态、`actor` 合法 |
| 剪枝记录（`## 反证实验` / `### 待测`） | 必需标题存在、代码块成对、反证结果非空（或明确标 `pending`） |
| 目录结构 | 状态目录名合法、同一 slug 不重复、无游离文件 |
| 模板 | 占位符完整、渲染后非空 |

#### 报错契约：明确指出"坏在哪、谁去修"

```
$ athena promote output-mutation
error: 解析 ~/.Athena/projects/fidus/terms.local.toml 失败
  ┌─ terms.local.toml:12:5
  │
  │     require_fields = [ "反证实验", "成本对账"  # ← 缺少闭合 ]
  │     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  │
= 原因：TOML 数组字面量未闭合
= 这是人工编辑导致的解析错误，Athena 不会自动修改你的文件。
= 请自行修复后重试；或用 `athena term edit` 走结构化编辑（推荐，可避免此类错误）。

（可用 `athena validate --explain` 查看完整校验报告）
```

**关键**：报错必须**精确定位 + 说明是人工编辑所致 + 明确不自动修复**。这避免两个陷阱：

1. **静默容错陷阱**：把坏字段当"默认"，结果规则悄悄失效（"文件能读、规则不生效"——正是 §2.1b 警告过的问题）
2. **自动修复陷阱**：工具擅自改用户的 `~/.Athena`，违反"允许人直接编辑"的承诺，且可能覆盖人的真实意图

#### 校验时机：只在"操作前"

- **读时校验，不后台巡逻**：不引入守护进程，只在命令实际用到该文件时校验
- **写操作不校验写入内容的结构**（`write` / `append` 是透传，允许人写任意文本）——校验只针对**被定义为结构化的那部分**（frontmatter、terms、模板、反证章节）
- **`athena validate` 是全量校验入口**：可随时手动跑，CI 可挂；它只报告、不修改

#### 与"文档即记忆、不回滚"的一致性

§1.3「文档即记忆」允许就地修订，本节能与之兼容：就地修订是**合法操作**，但修订后**必须保持可解析**。若改坏了结构，校验会在下次操作时立刻拦下——**错误被发现得越早越好**，而不是等到 `promote` 才发现整份剪枝记录格式全错。

这也解释了为何 `terms.local.md` 声明"避免手动修改"（§2.1b）：结构化字段用手动编辑**风险高**（少一个字符就解析失败），而用 `athena term edit` 走结构化接口可避免绝大多数解析错误。**推荐接口 ≠ 禁止直编辑**——直编辑合法，但要自行承担被校验拦下的代价。

> 待验证：Phase 3 dogfood 统计解析错误的实际频率与常见类型；若某类错误反复出现，优先改进模板/接口的防呆，而非放宽校验（校验宁严勿松）。

---

## 10. 实施

**Phase 0 — 地基（先做这个，因为它决定一切能否热插拔）**
- workspace 骨架：`athena-core` / `athena-domain` / `athena-rules` / `athena-context` / `athena-cli`
- `Store` + `GitBackend`（auto-commit）
- `terms.local.toml` 解析器 + `TermsRegistry` 热加载
- 内置模板（**`proposal.md.tpl` / `plan.md.tpl` 内含强制的 `## 反证实验` 章节骨架**（§13.1 内联单文件）；**`AGENTS.md.tpl` 是协议入口文件模板，命名可覆盖**）+ `~/.Athena/templates/` 覆盖机制
- **`athena-rules/falsification.rs`**：解析文件内 `## 反证实验` 章节 + 非空检查；**`athena-concurrency`**：`flock` 文件锁 + `content-hash` CAS + 事件日志写入（§13.2）
- **`athena-rules/empirical_falsification.rs`**：解析反证章节 + 非空检查；单测覆盖标题变体、空结果、代码块缺失（对应§11 反证实验暴露的解析鲁棒性）

**Phase 1 — MVP**
- `init` / `new` / `promote` / `complete` / `write` / `append` / `context`
- **`init` 的两件事**：① 实例化 `~/.Athena/projects/<name>/` 骨架；② **复制 `AGENTS.md.tpl`（协议入口文件模板）到项目根目录**（默认 `AGENTS.md`，可用 `--agents-file` 改 `CLAUDE.md` / `.cursor/rules/athena.mdc` 等；复制而非软链，避免泄露 `~/.Athena` 绝对路径）
- **`init --sync` / `--sync --dry-run` / `--sync --theirs`**：协议入口文件的显式更新路径（§1.1「更新路径」）；frontmatter 写入 `athena-version`，支持三路合并与冲突标记，默认不自动更新
- 四套内置模板 + `~/.Athena/templates/` 覆盖
- 路径白名单 + 自动 git commit
- `pitfalls` 两级；**"文档即记忆、就地修订、诱因入坑"**（§1.3）作为状态更新策略写入实现约定，不提供回滚命令
- **`validate` 硬规则前置**：`promote` 先校验反证实验，再移动文件

**Phase 1b — 术语接口（本提案新增，紧跟 MVP）**
- `athena term list / new / show / validate`
- `terms.md`（人读）+ `terms.local.toml`（机读，含 `require_fields`）
- 迁移首批术语：`池`、`草案`、`剪枝`、`Gate`、`零信任`、`反证实验`（来自 fidus）
- **规则生效闭环**：新增 `require_fields` → `athena validate` 自动识别 → 无需重编译
- **`[term.falsification]`**：配置豁免条件（纯文档类提案 → 反例检索）+ 逃生口策略（`--skip-falsification=<具象理由>` 留痕并标红）

**Phase 2 — 完整状态**
- `freeze` / `suspend` / `resume` / `archive`
- `validate` 完整性检查（规则来自 `athena-rules`，配置来自术语）
- `terms.md` 渲染进 context

**Phase 3 — 直接用（不是 dogfood）**

> **⚠️ 修正：本阶段不是"dogfood 验证"，判定标准也错了。** Athena 不是待验证的产品假设——**它本身就是我的工作流**（照抄 fidus 已跑通的流程，只是外置+固化）。把它写成"dogfood 后再决定是否采用"，等于假装先怀疑一个已经在用的东西。
>
> 因此本阶段定性为：**Migration + 边用边改**。迁移 fidus 现有内容，然后**在日常使用中直接演进**，遇到问题就地改协议/模板/命令，不做"验证通过后才启用"的门槛。

- **迁移**：把 fidus 的 `docs/proposals/` + `docs/drafts/` 迁进 `~/.Athena/projects/fidus/`（这就是首次真实使用，不是试验）
- **术语随用随录**：过程中冒出的 fidus 术语（`L9`、`Anchor`、`可定位性` 等）通过 `athena term new` 录入——**不顺手就当场改接口**，不等"验证结论"
- **边用边改的迭代回路**（比 dogfood 更直接）：
  1. 用 → 2. 卡住/绕过了 → 3. 立刻改协议文本或命令 → 4. 继续用
- **判定标准随之改变**（不是"验证是否值得做"，而是"用起来是否顺手"）：
  - 我会不会自然地想起来 `athena context`？不会 → 改 Sys 摘要的措辞
  - 剪枝是不是经常想跳过？是 → 说明它太重，改模板或放宽该场景
  - 术语是不是经常不够用？是 → 说明录入接口不顺手，改接口
  - `localize` / `compact` 是不是从没用过？是 → 说明是过度设计，考虑删除
- **记录**：每次"用起来不顺"都 `athena pitfall`——这些才是真实的迭代输入，比任何预想的验证指标更有价值

**Phase 4 — 规则可插拔（按需，不预做）**
- 项目级 `rules/` 自定义校验
- 代码生成：由 `require_fields` 生成严格 struct（类似 fidus 类型系统）
- 仅在使用中暴露真实需求后再做，避免预设计过度

---

## 11. 自审裁枝（本提案自己的）

> ⚠️ **本节本身就是反证实验的示范**：Athena 要求每个提案在离开 `pool/` 前做实机验证，本提案不能豁免。下面第 4 项即本提案的反证实验。

- **是不是在解决真正的问题？** 是——解决的是"AI 失忆 + 想当然"，不是"缺个 todo 工具"。
- **有没有更简单的做法？** 有——纯 shell 脚本别名。否决理由：无法做路径白名单与权限网关，AI 可直接绕过。
- **有没有更激进的做法？** 有——做成 MCP server 或 LSP 深度集成。否决理由：绑定 AI 运行时，违背"只依赖普适原语"。
- **过度设计吗？** 否。相比之前的枚举+事件溯源版，这一版已经大幅收敛。

### ⭐ 反证实验（§5.1a/5.1b 的自我应用）

> **假设**：把"原理可行性"提升为"原理实机验证（含主动反证）"，并以 `athena validate` 强制，能显著减少"前提是空的却死磕数天"这类损失。

**反证实验 1：fidus 的反证命令是否真的几秒出结果？**

```bash
$ find /usr/share/{wayland,wayland-protocols,wlr-protocols} -name '*.xml' | wc -l
42
# 逐条归类带 x/y 的 request/event → 三类，无"我在哪"
# xdg_toplevel.configure 事件实机打印 → [width, height, states]，无 x/y
# xwininfo -root -tree → 7 个辅助窗口（rootless XWayland）
# FIDUS_BACKEND=x11 cargo run ... → X11Error { error_kind: Match, major_opcode: 73 }
```

**真实结果**：五条全部在本机复现（Niri 26.04 / wayland-1 / eDP-1 1366×768），**单行命令、秒级出结果**。→ 假设成立：关键前提可被极低成本的实验否定。

**反证实验 2：Athena 的机制能否真的强制？**

| 角度 | 反证设计 | 预期若假设错 | 真实结果 |
|---|---|---|---|
| 留痕缺失是否可见 | 手动创建 `pool/x.prune.md` 留空 → `athena validate` | 标红警告并记录缺失 | **设计如此**（§6 validate 默认 warn，项目级 `mode=block` 时可拒绝） |
| 协议解析是否鲁棒 | 标题写成 `## 反证`（缺"实验"） | 解析失败漏检 | 需明确匹配策略：按标题前缀 + 首个代码块，**Phase 0 单测覆盖** |
| 人工编辑是否会破坏解析 | 人直接改 `terms.local.toml` 少个字符 | 工具静默容错或崩坏 | **设计如此（§9.1）**：操作前校验、报错交人修、绝不自动改文件 |
| "不可关闭"是否过度？ | 离线/无显示器环境无法实测 | 硬规则挡住合理场景 | **待定**：需 `--skip-falsification` 逃生？→ 反方证据成立，见下 |

**反证结果汇总**：

- 反证 1 → **假设成立**，且强化结论：fidus 的教训本可被几行命令避免。
- 反证 2 第三条 → **暴露真实失效边界**：CI 环境或无显示服务器的场景下，"必须实机反证"可能不可达。这与 fidus 的"诚实拒绝"（无法校准就不校准）同构——**这直接导致了 §5.1e 的口径修正：反证从"门槛"降级为"留痕"，`--skip-falsification=<具象理由>` 生成待测项而非静默豁免**。
- **失效边界**：本规则不适用于"纯文档/纯推理类提案"（无环境可测）；此时应把"反证"替换为"反例检索"（§5.1d 的检索型验证资产），并在 `terms.local.toml` 的 `[term.falsification]` 中配置豁免条件。这一条需在 Phase 0 实现前明确，否则 v1 会过度约束。

**结论（修正后）**：假设**成立，但"强制挡住"应改为"强制留痕"**。反证实验的价值不在被工具拦截，而在**被写出来、可被复核**——因此 `validate` 从"拒绝 promote"改为"标红警告 + 待测项登记"，`complete` 前必须清算所有 `pending`。反证机制仍是协议的核心，只是执行方式从 CLI 一刀切转为结构化留痕。这正是反证机制自我应用的成果：它把"听起来很对"收敛成了"成立，边界在此"。同时，反证机制本身也是协议的一部分（协议加载时分三级注入，§1.3），不是 CLI 的附加功能。

#### 反证实验 3：市场假设自身也需要被证伪

> §1.1b 断言"用户只有一类，A/B 是同一用户的两种部署形态"。这个断言若只是直觉，就违反了本提案第 2 条——**因此也要做实机反证**。

| 假设 | 反证设计（会观察到什么） | 预期若假设错 | 真实结果 |
|---|---|---|---|
| A、B 是同一市场 | 列举 A→B 与 B→A 各一例，看能否互换 | 两类用户的命令集/目录/门禁确实不同 | **可互换**：纯个人开 `--export-context` 即变偶发协作，命令集不变 |
| "个人偏执"与"仓库干净"是同一诉求 | 问：要后者是否必须先有前者？ | 存在"只要干净、不要门禁"的纯用户 | **未发现**：实际动机都是"不被 AI 坑"+ "不污染仓库"，同一根因 |
| 拆成两档会产生真差异 | 比较两档所需的 CLI 功能清单 | 两档功能清单显著分叉 | **清单完全一致**，差异仅在配置默认值 |
| 单人 vs 团队的边界才是真分水岭 | 验证 §1.1b 非目标清单 | 团队需求与单人需求本质同构 | **本质不同**：团队需 PR 门禁/并发/权限、共享协议上下文，超出 Athena 范畴，交给 OpenSpec |

**反证结果**：前三条支持假设——A/B 互换无损、动机同根、功能清单一致，故"只有一个市场"成立。第四条划定边界：当部署粒度跨过"团队协作"门槛，需求确实质变（多人共写、统一 constitution），此时应转介 OpenSpec/Spec Kit，而非扩展 Athena。**这与"个人↔偶发协作"的连续谱不同——团队是断点，不是开关。**

**结论**：§1.1b 的"单市场 + 协作开关 + 团队转介"成立，边界清晰。团队断点的本质是**"多人必须共享同一份协议上下文"**——这要求事实一致性、权限、ROT、检索污染、谁可写谁可覆盖，已经超出个人工作协议的范围。Athena 的协议入口只服务单个 Agent 加载上下文，不保证多人一致；此时应转介 OpenSpec/Spec Kit。

**同时，这条反证也产出差异化定位（§8.1）**：OpenSpec 是「规范评审流程」，管文档质量；Athena 是「AI 纪律协议」，管 AI 的推进方式。二者甚至互补——Athena 的状态库可在工作项需要正式评审时导出给 OpenSpec。因此"生态空位"论据应这样表述：**不是技术无人会做，而是主流产品的默认价值排序把"管 AI 行为"放在非默认位置**（团队治理优先级高于个人反证纪律）。这与"个人↔偶发协作是连续谱、团队是断点"的判断一致——空位真实，但不是空白。

这一条也示范了"自我应用"：Athena 要求提案反证自己的关键假设，市场定位也是假设，没有特权。

### 成本对账（§5.1 第 4 条）

| 维度 | 当前路径（提升门槛） | 更激进方案（做成证明助手 / 自动跑反证脚本） |
|---|---|---|
| 已投入 | 0 | 0 |
| 剩余成本 | 模板 + validate 规则 ≈ 1 个 crate 文件 | 沙箱执行、超时、依赖注入检测，量级大得多 |
| 成功概率 | 解析 Markdown + 非空检查，确定 | 自动判定"实验是否真跑了"本身难（可伪造输出） |
| 终局质量 | 强制留痕 + 人工判断，够用 | 自动化更稳但攻击面大、易被绕过 |

- **rejected_alternative**：自动执行反证脚本（更激进）——否决理由：`success` 可被伪造输出骗过，且引入了"必须能运行任意命令"的安全风险；**人工跑 + 强制贴真实输出 + validate 检查非空**是更简单的等价做法，终局质量足够。若未来需要更强约束，可在 `athena-rules` 加一个 `falsification_executable` 规则（§6b Phase 4），按需启用。

### 第 4 条（成本对账）本身会不会沦为形式主义？

这是本规则最大的自指风险——若对账表只填"略高/差不多"，就退化成原样。因此强制：**维持现状时必须给出可定位的一句具体理由**（本节已示范：`rejected_alternative` 写明"否决理由 + 未来启用路径"），且 `athena validate` 检查剪枝记录是否存在该字段与 `## 反证实验`。空泛的拒绝不通过校验。

### 术语"配置 + 热加载"是否过度设计？

走第 4 条对账：
- 已投入：0（Phase 0 起手就做，边际成本几乎为零）
- 剩余成本：把 `validate` 的硬编码字段抽成 `terms.local.toml` 读取，约一个 crate 的工作量
- 终局质量：术语是被坑出来的，**必然**会新增；若写死在 Rust，每次都要改代码+重编译+走发布，等于把"改协议"变成"改项目"——这正是 fidus 当初用类型系统解决的同一类问题
- **判定：做。** 本质是把 fidus 的"类型封死"从编译期平移到配置期，成本极低而解耦收益贯穿整个生命周期。若做成重量级 DSL 或动态加载才是过度设计——保持 TOML + 重新编译即可。这也印证 §1.3 的迭代优先级：协议文本可热改，CLI 才不必频繁改动。

### 模块化 workspace 是否过度拆分？

对账：
- 反方：v1 单 binary 也能跑，拆 6 个 crate 增加编译与心智负担
- 正方：唯一拆分理由是"便于调整流程"——若 `rules`、`context sections`、`render` 混在一个 crate，改 AI 上下文结构就要动核心；分离后改 section 不动校验
- **判定：做，但克制。** 严格按 §6b 五层；不预做 `rules/` 动态加载（Phase 4）、不做插件系统、不做 WASM。拆到"改配置不改代码"为止。

---

## 12. 为什么不只是一组 shell 脚本

> 这是本提案的终极压力测试。若答不好，整个 Rust 二进制就退化成"花哨的 bash 别名"。

### 12.0 CLI 是脚手架，不是镣铐

> **这是全提案最重要的一条定位声明。** 早期版本把它写成"CLI 防线只对愿意走 CLI 的 AI 有效"——那仍是**防守姿态**，隐含着"AI 是需要防备的对象"。按 §1.3 的正确原理（提示词工程 + AI 驱动），应当这样表述：

**AI 是状态机的主体，天然拥有对 `~/.Athena/` 的读写权**（§1.1，原理的必然推论，不是恩赐）。因此当 AI 直接用文件编辑能力改状态时，以下 CLI 机制不参与：

- 文件锁 — 不参与（锁只在 `athena` 命令里）
- content-hash CAS — 不参与（没走写命令，无人校验 hash）
- 路径白名单 — 不参与（canonicalize 只在 CLI 内）
- 事件日志 — 不记录
- `validate` 前置 — 不触发

**这不是"防线失效"，而是本来就不该指望它拦。** 把这些称作"防线"，等于默认 AI 是需要被防住的破坏者——**那与"提示词工程 + AI 驱动"直接冲突**。

**正确的定位**：

| 机制 | 真实作用（对主体的辅助） | 错误理解（防守姿态） |
|---|---|---|
| `athena write/append` | **顺手**：一次调用完成写入+留痕+commit | ❌ "唯一入口" |
| `validate` | **自检报告**：告诉 AI 当前状态完不完整 | ❌ "拒绝/拦截" |
| 文件锁 / CAS | **避免 AI 自相冲突的提示**（报冲突，由 AI 处理） | ❌ "防止并发损坏" |
| canonicalize | **防呆**：帮 AI 别把状态写到项目仓库里 | ❌ "权限网关" |
| 反证要求 | **强制留痕**：写出来即可，不阻塞 | ❌ "硬门禁" |
| git | **可查的历史**：事后回溯 | ❌ "审计追责" |

**Athena 的效力排序**（从根本到辅助）：

1. **协议文本**（Sys + 入口 + 模板）→ 决定 AI **想不想**做对 ← **唯一根本的效力来源**
2. **CLI 脚手架**（锁/CAS/白名单/校验）→ 让想做对的 AI **更容易做对** ← 对主体是辅助
3. **事后可查**（git + 事件日志）→ 做错了**能发现、能回溯**

> 一句话：**Athena 靠说服生效，不靠强制生效。CLI 是给 AI 的脚手架——它让愿意做对的 AI 做得更省力，而不是让 AI 不得不做对。**
>
> 反过来说：若哪天要靠"拦住 AI"才能维持流程，那说明**协议文本没写好**（§1.3 迭代优先级第一项），应当去改协议，而不是去加固 CLI。

### 12.1 先承认：骨架层面对 shell 完全透明

目录即状态、mv 即转换、git 即审计——这些 fidus 用纯文档就能做到，Athena 的骨架面对 shell 没有任何秘密：

```bash
mkdir -p ~/.Athena/projects/$1/{pool,working,finished,community}
mv ~/.Athena/projects/$1/pool/$slug.md ~/.Athena/projects/$1/working/
git -C ~/.Athena commit -am "promote $slug"
```

§9「为什么这样更简单」已承认这一点：无状态字段、无事件表、无数据库、无索引。**真正的对比不在骨架，而在四条 shell 守不住的线。** 以下均假设 AI 通过宿主工具（Cursor / Claude Code / 文件编辑 API）直接操作文件系统——这是 Athena 的部署前提（§1.1 允许 AI 读写 `~/.Athena/`），故"AI 会绕过 CLI"是必须正视的威胁，而非边界情况。

### 12.2 四条 shell 挡不住的线

**① 状态完整性：转换不被旁路**

shell 里"推进"就是 `mv`，但没有任何东西阻止 AI 这样做：

```bash
mv pool/x.md finished/x.md       # 跳过进行中，直接当作已结束
write_file("~/.Athena/.../pool/y.md", "...")   # 绕过 mv，直接创建
rm -rf pool/*                       # 误删不告警
```

`athena promote <slug>` 的价值不是"执行 mv"，而是**只允许通过命令转换**：canonicalize + 白名单 + 转换前置校验（`promote` 先 `validate` 再移动，§6）。shell 别名无法阻止宿主工具的文件编辑能力——除非 `chmod -w`，但那会违反 §1.1「允许 AI 动 `~/.Athena/`」。Rust CLI 给出的是**在"可读写状态库"前提下仍能守住转换路径**的唯一方式。

**② 留痕的结构化校验**

```bash
grep -q "## 反证实验" "$prune" || { echo "缺反证"; exit 1; }
[ -s "$prune" ] || { echo "为空"; exit 1; }
```

§5.1e 降级为留痕后，校验对象从"有没有章节"变成**结构化**的：解析 Markdown AST，定位 `## 反证实验` 标题后的代码块，区分"真实 `结果:` 行"与"`--skip-falsification` 生成的 `### 待测` 段"，检查 `pending` 清算状态，读取 `[term.falsification].mode` 决定 warn/block。正则能做到，但**每加一条规则重写一条 grep 管道**——这正是 fidus「能用类型封死的绝不靠约定」的直接论据。`athena-rules` 把它做成可插拔 trait（§6b），新增规则 = 加一个文件 + 注册一行。

**③ 路径白名单与越界防护**

```bash
resolve() { realpath --canonicalize-existing "$1"; }
case "$resolved" in "$ATHENA_ROOT"/*) ;; *) echo "越界"; exit 1;; esac
```

能写，但符号链接穿透（`~/.Athena/projects/x` → `/etc`）、`../` 拼接、`//` 折叠、相对路径解析——**边界 case 在 bash 里写对极难**，一次漏检就让 AI 把状态写进项目仓库或系统目录，直接违反核心隔离（§1.1）。Rust 的 `std::path` + 一次 canonicalize 即稳。这条与 ① 共同构成"**位置边界**"的强制实现（AGENTS.md ④）。

**④ 术语驱动的校验规则生成**

§2.1b 的核心是：改 `terms.local.toml` 的 `require_fields`，`validate` 自动识别，零代码改动。shell 要实现"配置驱动校验"，得内嵌一个 TOML 解析器 + 动态 grep 模板生成——可行性在，但已是在 bash 里重写一门 DSL，本质上就是重新发明 `athena-rules`。热加载（每次读取、不缓存）在 shell 里也要自己实现文件变更检测。

### 12.3 真正致命的一条：shell 救不了「协议不被读到」

这是最容易忽略、也最关键的一点——

**Athena 绝大部分约束力来自协议文本（Sys 摘要 + 入口文件 + terms.md + 模板），不是来自 CLI。**

```bash
athena promote foo
# 即使 CLI 什么都没做，只要 AI 已读过 AGENTS.md 并把协议内容带进上下文，
# 它就知道自己"应当先写剪枝章节、做反证留痕"
```

shell 脚本**无法让 AI 读到一段指令并自觉遵守**。协议文本是提示词工程产物，它要求的是"AI 理解并执行"，CLI 只是兜底。于是形成一组不对称：

- **协议写得好 + CLI 是 shell** → 仍然有效（AI 自觉走流程，偶发被 bypass 可被 `validate` 检出）
- **协议写得烂 + CLI 是 Rust** → 无效（工具再硬，AI 不知道为什么要这样）

这直接印证 §1.3「迭代优先级不可颠倒」：**先改协议文本与模板，再改执行边界**。CLI 是"手与锁"，但手和锁的价值取决于 AI 是否理解锁的意义——这个理解来自提示词，不是来自二进制。

#### 反证实验 4：⚠️ 协议加载本身是未验证假设

§12.3 的整段论证建立在「AI 会读到协议并自觉遵守」之上。**这是一个可能不成立的假设**（§1.1「协议加载是假设，不是保证」），若不验证，本节就是空中楼阁。关键在于：**协议内容放在哪一层，决定了它被读到的概率。**
>
> **fidus 的实证（本节被它修正）**：会话最开始对 AI 说——
>
> > *"我们的任务是继续推进这个项目，请读取 AGENTS.md 后向我提问以校准认知，然后我们会正式开始工作。"*
>
> **只需一次确定成功的加载。** 此后协议内容自然嵌入上下文全程有效，包含总结、折叠、续接会话——中途不会失效。因此不必担心"AGENTS.md 会不会被忽略"：**只要这一次读成功了，就不需要再担心失效。**




| 假设 | 反证设计（会观察到什么） | 预期若假设错 | 真实结果 |
|---|---|---|---|
| 项目入口自动加载 | 换个不支持 `AGENTS.md` 的宿主工具跑一次 | 协议完全不生效 | **依赖宿主实现，不受控**——故不能当第一道锁 |
| AI 读到后会遵守 | 给一份措辞弱的协议，看 AI 是否直接写代码 | 直接写代码、不进池、不剪枝 | **会发生**——提示词非强制指令 |
| 长会话是否稀释 | 长对话中段后触发 promote，看是否仍走流程 | 中段后忘记门禁 | **会稀释，但是可修复的稀释**——见下方 fidus 实证 |
| 显式要求能否一次成功 | 会话开始时对 AI 说"读 AGENTS.md 后向我提问以校准认知，然后正式开始工作" | 仍不读、仍不遵守 | **只需一次确定成功**：此后协议随上下文（含总结/折叠）续存到底，不必担心中途失效 |

**fidus 的实证（本节被它修正）**：

> *"我们的任务是继续推进这个项目，请读取 AGENTS.md 后向我提问以校准认知，然后我们会正式开始工作。"*

**协议加载只需一次确定成功**。AI 读完之后，协议内容会自然嵌入上下文全程有效——包括会话总结、折叠、续接。因此"AGENTS.md 会不会失效、中途会不会被忽略"，答案都是：**只要这一次读成功了，就不需要再担心**。中途稀释是真实存在的，但它可被一句固定指令修复（重说一次开场白即可），而非结构性缺陷。

**反证结果**：该假设**不成立为默认**（自动加载不可控、AI 可能忽略），但**可被一句开场指令变成成立**。因此它不是需要多层冗余去兜底的脆弱前提，而是**一次主动动作即可消除的风险**。

**修正后的表述**：

- **开场已让 AI 读协议 + 协议写得好 + AI 服从** → 有效（理想态，**且可持久**）
- **开场没说、但自动加载任一层生效 + 协议写得好** → 仍有效（自动加载是免费赠品，不是依赖项）
- **开场没说 + 自动加载全失效** → 无论 CLI 多硬，Athena 退化为"一个会移动文件的工具"，**协议价值归零**
- **协议被读到但写得烂** → AI 机械执行字面要求，不产生纪律

**结论**：Athena 的**第一风险不是 CLI 不够硬，而是开场没让 AI 读协议**。它同时是**最容易消除**的风险——一句固定指令即可，且只需成功一次。因此：

1. **使用契约改为"使用前先让 AI 读 AGENTS.md，一次即可"**——这是唯一必需的加载保证，不依赖宿主实现
2. **Sys 片段（`athena sys-export`）的真实作用是提醒人说出这句开场白**，而非指望系统提示自己把规则灌进去
3. Sys 保持极短 + 用 hook 在关键动作前强制 `athena context` / `validate`（确定性拦截不交给提示词）
4. AGENTS.md 只做项目接口、只放薄层，详情按需拉取
5. 长会话/续接会话中若协议被稀释，**重说一次开场白**（成本极低、终身只需少数几次）
6. 不再把"入口文件时刻有效"当前提——Athena 不需要它时刻有效，只需要成功加载一次

> 这一条同样是"自我应用"：§12 声称协议文本是核心资产，那么这个声称的前提（协议会被读到）也必须先被验证。fidus 的实证给出了答案：**可以，而且一次就够。**
>
> 顺便反证了 §12.0"CLI 防线失效"与本节旧版"入口文件可能失效"的过度悲观——**入口文件不需要时刻有效，只需要成功加载一次。**

#### 反证实验 5：独立 prune 文件能否在并发下保持一致

> §13 的整个并发模型建立在"剪枝记录内联进提案/方案文档（一个 slug 一个文件）"之上。这个数据结构选择本身必须被反证——**若独立 `<slug>.prune.md` 在并发下也能保持一致，则 §13.1 的修正不必要，§13.2 的锁粒度也无需绑定到 slug。**

| 角度 | 反证设计（会观察到什么） | 预期若假设错 | 真实结果 |
|---|---|---|---|
| 双文件移动是否原子 | 两个会话并发 promote 同一 slug，观察 md/prune 是否曾处于"md 已移、prune 未移"的中间态 | 存在中间态 → 独立文件方案破产 | **必存在**：`mv` 两次无事务，race 可复现 |
| 锁粒度是否被绑架 | 独立文件下，对 `pool/foo.md` 与 `pool/bar.md` 并发 promote，测量是否互相阻塞 | 互相阻塞 → 并发度归零 | **互相阻塞**：跨文件锁难做细粒度，退化为项目级锁 |
| 配对约定是否脆弱 | 手动 `cp` 提案、IDE 生成 `.swp`、rename 成 `foo-v2.md`，跑 `validate` | 出现孤儿文件 | **会出现**：`*.prune.md` glob 依赖文件名约定 |
| slug 重命名是否断配对 | rename 后校验器能否自动找回配对 | 需人工修复 | **需人工**：隐式配对无 schema 约束 |

**反证结果**：第 1、2 条即可推翻"独立文件可行"——**双文件移动的非原子性是结构性缺陷，不是实现瑕疵**，无法靠校验补。**故 §13.1 成立：剪枝记录必须内联**，`<slug>.prune.md` 形式作废。

**推论**：这一结论反过来限定了并发模型的**上限——锁粒度最多到 slug**。若未来需要"同一文档内段落级并发"（两人同时改同一提案的不同章节），文件锁已无法支撑，需引入 CRDT/OT——**这超出 Athena 范围，交给专业协作工具（如基于 Git 的实时协同）**，Athena 只保证 slug 级串行。此边界写入 §1.1b「不做清单」。

> 这一条是 §13 对自身数据结构的前提反证：它证明了"内联单文件"不是风格偏好，而是**强一致并发的必要前提**。

### 12.4 对照表：shell 能替代什么

| 层 | shell 能替代？ | Athena 的做法 |
|---|---|---|
| 目录骨架 / mv / git | ✅ 完全能 | Rust 只收敛成稳定命令名 |
| **状态完整性约束** | ⚠️ 难（防不住宿主工具直写） | canonicalize + 白名单 + 命令是唯一转换入口 |
| **结构化留痕校验** | ⚠️ 能，但每规则重写 grep 管道 | `athena-rules` 模块化 + 术语驱动 |
| **并发互斥（§13）** | ❌ 难（需 flock + CAS + content-hash，且依赖单文件原子性） | slug 级文件锁 + compare-and-swap + 事件日志；**前提是剪枝记录内联（§13.1）** |
| **路径越界防护** | ⚠️ 边界 case 易错 | `std::path` 一次 canonicalize |
| **术语热改 → 校验自动生效** | ⚠️ 要重写 TOML+DSL | TOML → 自动注册 |
| **协议被 AI 读到并遵守** | ❌ 完全不能 | AGENTS.md + terms.md + 模板（核心资产） |

### 12.5 结论：Athena 不是状态机程序，而是「协议 + 最小执行边界」

若 Athena 只是"管状态的 CLI"，它就是过度设计的 shell 别名；但 Athena 实际是：

> **一份工作协议，其约束靠提示词生效、靠最小 Rust 边界兜底；而提示词只需成功加载一次——那句固定开场指令（"读 AGENTS.md 后向我提问以校准认知，然后正式开始工作"）让 AI 读一次入口文件，之后协议内容随上下文续存到底，不需要任何冗余去保证它留在上下文里。**

shell 脚本能替它做骨架，但替不了"**让 AI 自觉遵守纪律**"这件事。Rust 代码是实现细节（§1.3），真正不可压缩的是协议文本。

这同时给出 Athena 的最小实现判据：**只要协议文本能被 AI 加载、且存在哪怕一个命令守住位置边界（§1.1）与留痕完整性（§5.1e），Athena 就已成立**；其余命令（`freeze`/`archive`/`term`）都是 ergonomics，不是本质。反过来，若只有 Rust CLI 而没有被 AI 读到的协议，Athena 就退化成空壳——这正是 §1.3「不应颠倒」的极端情形。

> 本节本身是一次自我反证：若有人能写出一组 shell 脚本让 AI 同样自觉走流程、同样守得住位置边界与留痕，**则 Rust CLI 即可退化为薄壳**；目前看，前四线中 ① ③ ④ 要求的状态完整性 shell 难以低成本保证，而 ⑤ 完全无解——故 Rust 最小边界仍值得保留。此判断待 Phase 3 dogfood 复核。

---

## 13. 多会话并发与多项目

> **前提**：Athena 的状态机是"目录即状态"，而 `~/.Athena` 是普通目录。两个 Cursor 窗口、一个 Claude 长任务加一次手动编辑，本质上就是**多个进程共享一个文件系统数据库**——文件系统没有事务。因此并发控制不是可选项：**记忆必须尽可能保持一致**，代价是锁开销与"另一个窗口被挡一下"。脏一次就可能让 AI 基于错误事实做方向性决策，这比"慢一点"贵得多，与 §9.1「解析错误宁严勿松」同一条价值观。
>
> 本节由两条相互咬合的判断驱动：
>
> 1. **记忆应尽最大程度保持一致**（强一致，非宽松协作）；
> 2. **剪枝记录不应独立成文件**——它必须内联进提案/方案文档本体。
>
> 第二条是第 1 条的**前置条件**。若保留 `<slug>.md` + `<slug>.prune.md` 的双文件配对，并发方案要么退化为"项目级全局锁"（锁粒度被绑架到整个项目，不同 slug 也无法并行），要么在"md 与 prune 的原子移动"上持续打补丁。因此 §13.1 先修正数据结构，§13.2 才在其上建并发层。

#### ⚠️ 自我质疑：本节可能是全提案最典型的过度设计

> 必须把这条质疑写在最前面，否则下面几千字会被误当成"一定要实现的东西"。

§13 的四层并发模型（文件锁 → content-hash CAS → session-id 语义 → 事件日志）**很可能是过度设计**，理由：

1. **真实并发频率可能极低**。Athena 的主场景是**单人 + AI**：同一时刻真的会有两个会话改**同一个 slug** 吗？大概率不会——更常见的是改不同 slug（本来就并行，无需锁）或顺序操作（无需锁）。
2. **锁保护的是"AI 愿意走 CLI"这个前提**。若 AI 直接编辑 `~/.Athena`（§1.1 允许），锁根本不参与——**CLI 防线只对愿意走 CLI 的 AI 有效**（见 §12.0）。对绕过者，四层模型形同虚设。
3. **事件日志 + session-id 是审计增强，不是正确性必需**。它们让冲突可诊断，但即使没有，记忆也不会更脏——只是"脏了不知道谁弄的"。
4. **成本不低**：`flock` + CAS + hash 维护 + 事件日志轮转，是 CLI 里最复杂的部分，而它保护的是一个**低频且本可宽容**的场景。

**因此本节的正确读法是：一个"若需要则这样做"的设计预案，不是 MVP 必须。**

**降级路径（按此顺序砍）**：

| 保留到哪层 | 得到什么 | 失去什么 | 建议 |
|---|---|---|---|
| **只做 §13.1 内联** | promote 原子、无孤儿文件、锁粒度可到 slug | 并发一致性无保证 | **MVP 最低限度**：先只做这个 |
| + 文件锁 | 同 slug 串行，防覆盖 | 跨边界（容器）仍失效 | 单人够用 |
| + content-hash CAS | 防"基于旧版本写入" | 偶尔误报阻断 | 谨慎：可能烦人多于有用 |
| + session-id / 事件日志 | 冲突可诊断、可审计 | 复杂度、日志轮转 | **最可能砍掉的部分** |

**判定方式**：按 §10 Phase 3「边用边改」——**用起来从不触发冲突 → 就停在最低限度**；真的被并发坑过一次 → 再加下一层。**不要让预案变成预支的复杂度。**

> 一句话：**内联（§13.1）是设计正确性，必做；并发四层是风险预案，按需。** 二者的必要性完全不同，不要因为都写在 §13 就被同等对待。

### 13.1 剪枝记录内联：一个 slug = 一个文件

#### 问题：独立 prune 文件在并发下每一处都会出事

原设计把 `<slug>.md`（提案）与 `<slug>.prune.md`（剪枝记录）分开存。单会话下没问题，**多会话下它与"一致"直接冲突**：

1. **原子性破产**：`promote foo` 的语义是"移 `<slug>.md` 且要求 `<slug>.prune.md` 已就绪"——这是**两个独立文件的移动**，文件系统不保证原子。A 会话把 `foo.md` 移到 draft 之后、对 prune 做最终校验之前的窗口里，B 会话的 `validate` 看到"draft/foo.md 存在但 prune 状态不一致"。
2. **rename 竞态翻倍**：单文件 `mv` 原子，两文件就得"先 mv A 再 mv B"，中间态对外可见。加锁能解决，但**锁粒度被迫上升到整个项目**——跨文件的锁很难做细粒度，这一下把并发度打回地板。
3. **配对靠隐式约定（文件名）**：任何只动其一的操作（手动 `cp`、备份工具、IDE 的 `.swp`、半成品 rename 成 `foo-v2.md`）都会制造孤儿或半孤儿。`validate` 得不断扫"有没有只有 md 没有 prune 的孤儿"——这是用校验补设计缺陷。
4. **promote 前置条件被拆到两处**：校验器要先打开 md、再"去隔壁找同名 prune"、再解析 prune。一旦 slug 重命名，配对即断。

> 这条与 fidus 的 hardlink/reflink 教训同构：**让正确性依赖于"两个 inode 永远同步存在/不存在"的隐式约定，本身就是脆弱设计。** §13 把它显式反证掉了（见 §11 反证实验 5）。

#### 修正：剪枝记录是文档内的章节

**一个 slug = 一个文件**。剪枝记录（自审裁枝、反证实验、更优雅/脑洞方案、成本对账、决策）作为**固定章节**内联进该 slug 的提案/方案文档：

```markdown
---
slug: output-mutation
kind: draft                # 文档类型：proposal | draft | plan
status: working            # 推进状态（冗余快照，真值在目录）：pool | working | finished | community
project: fidus
updated-by: sess-7c3f
updated-at: 2026-09-19T17:40:00+08:00
content-hash: sha256:9f3c...    # 并发 CAS 用（§13.2 第 2 层）
---

# output-mutation

## 目标 / 动机 / 约束
...

## 自审裁枝
...

## 反证实验                 ← 剪枝记录内联在此
### 待测（`--skip-falsification` 生成）
- [pending] ...（具象理由）...
### 结果
- [2026-09-18 11:02] xwininfo -root -tree → 7 个辅助窗口（rootless XWayland）
- [2026-09-19 09:15] ...

## 更优雅 / 脑洞方案
...

## 成本对账
...

## 决策
采用方案 B；理由见成本对账。

## 决策日志                    ← 快速通道的追加落点（§5.4）
- [2026-09-19 14:02] quick: 修正术语拼写（"裁枝"→"剪枝"）— sess-2a1b
```

#### 收益（不只是修并发，是把多处设计变干净）

1. **promote 原子**：移一个文件，章节自然跟着走，不存在"md 与 prune 不一致"
2. **锁粒度可细化到 slug**（强一致下能保住的最大并发度）：`~/.Athena/.locks/<project>/<slug>.lock`，**不同 slug 完全并行，仅同一 slug 串行**
3. **配对关系由"文件系统约定"升为"文档结构"**：不再有孤儿文件，`validate` 只解析单个文件内部章节
4. **归档天然保留**：文档随 `pool→working→finished` 一路走，**剪枝记录与决策历史自动归档**，无需单独处理"prune 文件去哪"
5. **与人直接编辑的契约更清晰**：§9.1「报错交人修」——单文件的错误定位比"两个配对文件"简单得多

#### 代价（必须诚实面对）

- **文件会变长**：大提案的 plan + 完整反证实验可上千行 → 用 heading 导航/折叠解决，AI 本就按标题切分
- **`append` 语义重定义**：`athena append <slug> -c "..."` 改为"追加到该文件的 `## 决策日志` 段"，靠解析定位插入点（复用 §9.1 的解析器），而非追加到文件尾或独立文件
- **`*.prune.md` 的 glob 规则全部失效**：§6 validate 第 2 条改为"文件内 `## 反证实验` 章节存在且非空"

#### 连带修改清单（本节落地时一并执行）

| 位置 | 变更 |
|---|---|
| §2.1 模板 | `prune.md.tpl` 从独立文件 → 并入 `proposal.md.tpl` / `plan.md.tpl` 的内联章节模板 |
| §5.1 剪枝 | "剪枝记录"改称"剪枝章节"；promote 前置条件改为"文件内 `## 反证实验` 章节就绪" |
| §5.1e 留痕降级 | 待测项写入文件内 `### 待测` 段，不再有独立 prune 文件 |
| §5.4 快速通道 | 留痕追加到 `## 决策日志` 段，不再 `<slug>.quick.md` |
| §6 validate | 规则 1/2 改为"文件内章节检查"，删除所有 `*.prune.md` glob |
| §9.1 解析校验 | 新增：`slug == 文件名`、`status` 合法、章节齐全、`content-hash` 自洽 |
| §12 shell 对比 | 新增"并发互斥"一条：shell 要实现 flock + CAS + hash 校验才等价 |
| AGENTS.md ③ | 明确"每个工作项一个文件"；禁止手动拆分 md / prune |

### 13.2 并发模型：四层

内联单文件之后，并发方案才站得住。四层由下到上：

```
会话 S1 ─┐
         ├─→ 项目索引锁（短持有）──────┐
会话 S2 ─┘                              ├─→ 实际文件操作（flock + CAS + hash）
会话 S3 ─→ 只读操作（context/log）→ 不加锁，读快照
```

**第 1 层：文件锁（基础设施，必须）**

- 每个写操作对 `~/.Athena/.locks/<project>/<slug>.lock` 取 `flock` 独占锁
- **锁粒度 = slug**（单文件设计的核心红利）
- 跨进程有效：不同 AI 工具、不同终端、不同 shell 都算同一把锁
- 锁文件本身用 `O_CREAT` + `flock`，不依赖 NFS-safe 语义（本地 `~/.Athena`）

#### ⚠️ 已知失效场景：容器内会话 vs 宿主机会话（同一 `~/.Athena` 经 bind mount 共享）

**这是 `flock` 模型的真实边界，必须显式声明，不能假装锁一定有效。** 当容器内的 AI 会话（如 devcontainer、docker run -v、Podman rootless）与宿主机上的会话通过 bind mount 操作**同一份 `~/.Athena`** 时，锁可能失效：

| 挂载/文件系统 | `flock` 行为 | 后果 |
|---|---|---|
| **本地 ext4/xfs/btrfs + bind mount**（最常见） | 通常正确传递到同一底层文件系统 | ✅ 锁有效 |
| **NFS / SMB 网络挂载** | `flock` 依赖服务端 lockd，可能不生效或语义退化 | ❌ 锁可能失效 |
| **9p / virtiofs**（某些 VM、容器、WSL 方案） | `flock` 支持有限或不一致 | ❌ 锁可能失效 |
| **overlayfs 上层**（修改仅在上层可见） | 锁文件可能在不同层各有一份 | ❌ 两边看不到同一把锁 |

**失效后果**：两侧各自持"锁"却互不知情 → 并发写同一 slug → 后写覆盖先写，且 CAS 也救不了（因为两侧 hash 摘要表各自读到的是自己的版本，直到下次刷新才发现）。**这正是 §13.1 强调"记忆一致"时最危险的漏洞——它静默发生，不像解析错误会报错。**

**缓解措施（按优先级）**：

1. **检测并警告**：`athena` 启动时探测 `~/.Athena` 所在文件系统类型（`statfs`）；若为非本地文件系统（NFS/9p/overlay 等），`validate` 与写操作前输出**显式警告**：
   ```
   ⚠️ ~/.Athena 位于非本地文件系统（9p），flock 跨边界可能失效。
      容器内与宿主机并发写同一 slug 时，强一致不保证。
      建议：同一时刻只让一侧写 ~/.Athena，或改用 --force 后人工核对。
   ```
2. **兜底互斥（不依赖 flock 语义）**：用 `O_CREAT|O_EXCL` 原子创建锁文件 + **TTL 心跳**（锁内写 `pid/hostname/timestamp`，超时视为过期可抢占）。在 flock 不可靠的文件系统上，这比纯 flock 更robust，但仍是"尽力"而非强保证。
3. **协议层提示**：`context` 输出的 hash 摘要表附带"本会话读取时的文件系统类型"，让 AI 知道当前一致性保证的等级。

> **诚实结论**：**同一宿主机内**（多终端、多 AI 工具）→ 强一致有保证。**跨越文件系统边界**（容器/VM/网络挂载）→ **只能"尽力"，不保证强一致**。这不是能靠代码修复的缺陷，是 `flock` 跨边界的固有限制，因此必须写进文档让使用者知情，而不是假装保证。
>
> 实践建议：**同一时刻只让一侧（容器内 或 宿主机）写 `~/.Athena`**，另一侧只读。这与 §13.5 跨机"无持续关系"的判断一致——**凡是跨越了隔离边界的两方，都不应声称强一致**。

**第 2 层：compare-and-swap by `content-hash`（防过期写入）**

- frontmatter 的 `content-hash` 是**写前快照**的哈希
- 每次 `write`/`append`/`edit` 提交时携带"我读到的 hash"；若当前 hash 已变 → **拒绝并精确报错**（不自动合并）

```
error: 文件 pool/output-mutation.md 已被其他会话修改（sess-2a1b，3 分钟前）
  当前 hash: sha256:9f3c...
  你基于的: sha256:1e7a...
= 请重新 `athena context` 加载最新版本后再编辑。
= 若确需覆盖，用 --force（将写入一条冲突事件，不静默）
```

- `--force` 不是豁免，是**显式留债**：往事件日志写 `conflict-overwrite`，`validate` 标红

**第 3 层：session-id 语义（协议层，让 AI 自感知）**

- `athena context` 返回时附带 `session-id`，以及**每文件的当前 hash 摘要表**
- AI 后续操作自动携带 session-id；CLI 据此提示"该文件已被其他会话改动"
- 解决"上下文过期"——这是 **AI 独有的问题，纯 git 方案没有这层**（§12.3 的延伸）

**第 4 层：事件日志（审计 + 冲突诊断）**

- `~/.Athena/projects/<name>/.athena/events.log`：每次**写**操作 append 一行
  `ISO时间 | session | action | slug | before-hash | after-hash`
- 只读操作（context/log/只读 validate）**不写日志**
- `context` 可选包含最近 N 条事件，让会话知道"别的会话在动什么"

> 读不加锁，写才锁——这是关键。`athena context` / `validate`（只读）/ `log` 都是快照读，多会话并发读零开销。**开 5 个窗口看 context 无任何锁竞争**，只有 promote/write/append/edit/quick/freeze 走锁。

### 13.3 锁策略：短锁（操作级）为主，长锁仅作提示

**已采用策略 A（短锁，操作级）**：

- 每次 CLI 调用自己加锁、做完即释放
- "读文件 → AI 思考 30 秒 → 写文件"之间锁已释放，故**仍需第 2 层 hash CAS 兜底**（短锁不消除 TOCTOU，只缩小窗口）
- 简单，无死锁风险

**策略 B（长锁，会话级）** 不作为默认（**已否决为默认**），仅作为 `validate`/`context` 的**提示信息**：

```
ⓘ slug output-mutation 正被 sess-7c3f 持有（已 2 分 10 秒，基于事件日志推断）
  如需独占，可 `athena lock output-mutation [--ttl 10m]`，超时自动释放。
```

理由：让 AI 显式管理 `lock`/`unlock` 会引入"忘了 unlock 即死锁"，需 lease 超时——收益不足以覆盖复杂度。**提示信息足够让协作者手动协调**，无需强制长锁。

### 13.4 多项目：各自独立，互不干扰

`~/.Athena/projects/<name>/` 下各项目是**独立状态树**，互不干扰，锁空间天然隔离（锁路径含 `<project>`）：

- **项目级 pitfalls**：锁在项目文件，同上
- **`context` 合并顺序**：全局 → 项目级 → 会话级，后者覆盖前者；合并发生在每次 `context` 调用时（读快照，无锁竞争）

> 本节刻意简短：**多项目不存在并发难题**——锁路径含项目名即天然隔离，跨项目只在 `context` 合并时相遇，而合并是只读快照。真正的难点是**多会话**（§13.2）与**跨机**（§13.5），后者见下。

### 13.5 跨机（新机器初始化）：快照传承 + 本地化清洗

> **核心区别**：跨机不是"同步"，而是"**一次性传承 + 本地演化**"。Athena **没有多机一致性的概念**——上一轮设想的"git remote 跨机同步 / 本地强一致 + 跨机最终一致 / 脑裂检测 / merge 策略"全部**作废**，因为它们建立在错误的"持续关系"假设上。本节取代之。

#### 为什么不能自动同步

同一件事在不同机器上可能是**相反**的教训：

- 笔记本因 GPU 显存不足记录 pitfall「batch size 调 8 以下」→ 同步到 8 卡 A100 服务器，AI 会基于错误前提做保守决策
- CI 机网络快，记录「直接拉取远端依赖」→ 同步到笔记本，AI 反复超时重试
- A 机 `plan/foo.md` 推进到方案阶段，不代表 B 机的 foo 也到了——**状态是环境相关的时间线**

**强行同步 = 制造幻觉**："我知道另一台机器上的一切"，实际不知道，且知道的是错的。故 Athena 的跨机策略不是"解决冲突"，而是**让冲突根本不存在**——物理隔离 + 一次性适配。

#### 模型：整个 `~/.Athena` 是一个仓库，复制即传承

`~/.Athena` 是**一整个**工作协议存档（协议 + 记忆 + `.git/`），不是按主机名分区的集合。新机器 B 从已有机器 A 获得它的方式：**直接复制整个目录**——

```bash
# 在机器 A 上打包（含 .git 历史）
tar -C ~ -czf athena-bundle.tar.gz .Athena
# 在机器 B 上解包到 ~/.Athena，然后执行本地化清洗
athena localize --from <hostname-A> [--dry-run]
```

复制的是**整个 `~/.Athena`**（含 `.git/` → 保留完整历史），不是某个子目录。复制完成后，B 拥有 A 的全部记忆与状态，**但处于"未适配"状态**。

#### 清洗：复制时的人工适配（关键步骤）

复制后必须跑 `athena localize`，由 AI **逐项审阅并按建议标记**，最终由**人拍板**执行。清洗动作是修改 `~/.Athena`，须走权限网关（用户明确指令 + `athena write`），AI 不擅自删。

**分类处置表**：

| 记忆类型 | 处置 | 理由 |
|---|---|---|
| 与硬件/环境强相关的 pitfall（GPU/CI/网络/路径） | **挂起或删除** | 不适配 B |
| 与工作流/方法论/错误类别相关的经验 | **全保留** | 跨机通用 |
| 各项目状态（pool/draft/plan） | 保留，标记「从 A 迁移」 | 继续推进需上下文 |
| `finished/` | 全保留 | 历史是参考 |
| `updated-by` / session-id | 重置为 B 的会话 | A 的会话已失效 |
| 事件日志 | 保留（清洗动作追加一条） | 审计可追溯 |

清洗本身产生一条事件：`localize: 从 <A> 迁移，保留 N 条 / 挂起 M 条 / 删除 K 条`。

#### 处置方式：挂起优先于删除

默认**挂起（加 `superseded` 标记）**，保留为决策参考，不彻底删除——除非用户明确要干净。被挂起的 pitfall 仍可被 `athena context` 检索（带标记），但不自动注入默认上下文。

#### 清洗之后：两机完全独立

清洗完成后，**A 与 B 不再有任何关系**。此后各自演化，互不知道对方存在。Athena **不提供**持续同步、`pull`/`push`、双向 merge——任何"想让两台机器共享新坑"的需求，都退化成**再做一次复制 + 清洗**（人驱动的单向传承），或 `athena pitfall export/import` 的单条手动搬运（见下）。

> 这是刻意的设计：**把"该不该共享"的决策成本，从机器转移给人**。机器只负责传承与隔离，人不应该被自动同步绕过。

#### 可选：单条手动搬运（不做自动同步）

#### 明确：允许 AI 同步「有必要同步的坑」

**修正上一版的过度收缩。** 之前写"默认不提供、Phase 4 视需求决定"，把跨机搬运说得太死了——实际上**跨机最该同步的恰恰是坑**（尤其是方法论/错误类别的坑），因为它们是"用教训换来的、跨环境通用的资产"，不搬才是浪费。

因此明确规则：**AI 可以主动识别并同步"有必要同步的坑"，但必须人确认，且绝不演成自动双向同步。**

**AI 判什么该同步**（与 `localize` 清洗分类互为逆操作）：

| 该同步（跨机通用） | 不该同步（环境绑定） |
|---|---|
| 方法论/流程类教训（"AI 说完成但没跑测试"） | 硬件相关（GPU 显存、CPU 核数） |
| 错误类别/反证模式（"看起来合理的值"陷阱） | 路径/网络/依赖源相关（CI 能拉、本地不能） |
| 工具与 API 的坑（某库某版本的真实行为） | 本机配置与环境变量 |
| 协议本身的改进（术语、模板、剪枝字段） | 项目状态与进行中的工作项 |

**机制**：

```bash
# A 机：AI 审阅全局坑，标出"跨机通用"的候选
athena pitfall sync-plan --to-machine B       # 输出建议同步清单 + 每条理由，不执行
athena pitfall export --sync-plan <plan>      # 人确认后导出（含来源机器、日期、通用性理由）
# B 机：导入，走正常校验（§9.1）；导入时同样由 AI 建议"哪些仍需挂起"，人拍板
athena pitfall import <bundle> [--dry-run]
```

**三条硬约束**（防止它退化成同步）：

1. **人确认**：AI 只出建议清单与理由，**执行必须由人触发**（与 `localize`、压缩一致：AI 建议、人拍板）
2. **单向、点对点**：A→B 一次性导出导入，不是双向、不是持续、不建立关系
3. **导入即重审**：B 上导入后仍需 `localize` 式审阅——因为"通用"是 A 的判断，B 才有权决定它在本机是否适用

> 一句话：**坑值得搬，所以让 AI 帮你挑；但搬不搬、留不留，人说了算。** 这与 §13.5 主模型不冲突——整体仍是"无持续关系"，只是**单向资产搬运被显式允许**，且由 AI 降低挑选成本。

#### 与 §13.2 的关系：两层完全正交

#### 与 §13.2 的关系：两层完全正交

| 层 | 范围 | 机制 |
|---|---|---|
| 单机多会话 | 一台机器内多个进程/窗口 | 强一致：文件锁 + content-hash CAS（§13.2） |
| 跨机 | 两台机器之间 | **无持续关系**：一次性复制 + 清洗（本节） |

两层**不交互**：B 上跑 `athena localize` 期间，B 本机的多会话并发照常由 §13.2 锁保护；清洗过程本身是一次写操作，受文件锁管辖。**没有"跨机锁"这种东西**。

### 13.6 git 的角色：审计层 + 传承载体，不做并发控制

`~/.Athena` 是 git 仓库。**git 不提供同工作区多进程的并发互斥**——这正是 §13.2 第 1 层锁不可替代的原因。git 的角色收敛为二：

1. **审计层**：每次操作一个 commit，events.log 是冗余审计
2. **传承载体**：`~/.Athena` 含 `.git/`，整目录复制即完整传承（含历史，见 §13.5）

> ⚠️ **git 不做跨机同步，也不依赖 git merge 解决 Athena 并发**。自动 merge 对锁文件/二进制是灾难，且语义上"两个会话各自 commit"的先后顺序 ≠ 决策先后。事件日志才是真实顺序。冲突的最后兜底（两会话都 `--force` 覆盖）：git 历史保留双方版本，可 `git show` 找回——这是"文档即记忆、不回滚"（§1.3）之外的最后一道，**不回滚业务状态，但历史可追溯**。

### 13.7 与既有设计的一致性自检

| 既有原则 | 本节的落实 |
|---|---|
| 目录即状态（§9） | 不变；单文件 + 文件锁 = 状态机 + 互斥，仍无数据库 |
| 文档即记忆、不回滚（§1.3） | 事件日志 + git 历史提供"可查的历史"，但业务状态只向前修订；**跨机传承靠复制快照，不靠回滚/同步** |
| 协议入口加载三层冗余（§1.3） | session-id 是第 3 层的工程化延伸 |
| 解析校验报错交人修（§9.1） | 并发冲突同样"精确报错、绝不自动合并/静默"；`localize` 清洗同样由人确认 |
| 按改动量级匹配记录重量（§5.4） | 快速通道走内联 `## 决策日志`，不新增文件，与内联设计一致 |
| **新**：快照传承 + 本地演化 | 与新项目 `init` 从主脉复制快照**完全对称**（§1.1）——**统一的模型是"传承 + 本地演化"**，传承一次性、演化永久 |

> 待验证（Phase 3 dogfood）：① 同一 slug 高并发下的锁竞争实测（预期极低，因锁粒度 = slug）；② hash CAS 误报率（AI 读 context 到写之间被人改了 → 是否过于频繁地阻断）；③ 事件日志大小上限与轮转策略；④ `athena localize` 的 AI 分类准确率——哪些 pitfall 会被误判为"通用/环境相关"，是否需人工复核兜底（预期：**必须保留人工确认环节，AI 只出建议**）；⑤ 复制含 `.git/` 时的仓库体积与 history 膨胀；⑥ 容器/VM 场景实测 flock 行为，验证 §13.2 失效表与警告是否准确。

### 13.8 责任边界：哪些情况 Athena 不负责

> **必须显式声明边界，否则会被误当成设计缺陷来"修"——而它们根本不是 Athena 能修、也不该修的。**

Athena 是一份**工作协议 + 执行边界**，它约束的是"AI 如何推进工作"。以下几类情况**本质不是设计问题，Athena 明确不负责**：

| 情况 | 本质 | Athena 的态度 |
|---|---|---|
| **用户催促 AI 跳流程**（"别管那些了，直接改"） | 人主动绕过了协议 | **不负责**。协议约束 AI，不约束人；人有权要求跳过 |
| **用户在对话里直接给结论**（"就用方案 B，不用论证"） | 人提供了权威输入，跳过了剪枝 | **不负责**。但 AI 应记录"结论由人给定，未经剪枝"，留痕即可 |
| **用户自己绕过 CLI 直接改 `~/.Athena`** | 允许直编辑（§1.1）的自然结果 | **不负责阻止**，但校验会在下次操作时拦下（§9.1） |
| **人删掉了协议入口文件** | 人有权不用这套协议 | **不负责**。Athena 对此无感知也不该强行恢复 |
| **模型能力不足**（读不懂协议、不会用命令） | 宿主模型的问题 | **不负责**。迭代方向是改协议措辞（§1.3），不是怪工具 |
| **跨文件系统边界的并发**（容器/VM/网络挂载，§13.2） | `flock` 固有限制 | **不承诺强一致**，只做检测+警告+兜底，并明确告知 |
| **多人团队强治理**（PR 门禁、权限、审批流） | 超出个人协议范围 | **不负责**，转介 OpenSpec/Spec Kit（§8.1） |

**为什么必须写这一节**：若不声明，这些情况会被反复当成"Athena 的漏洞"上报——比如"用户催了一下 AI 就没走流程，Athena 没拦住"。但 Athena 的约束对象是 **AI 的默认行为**，不是人的意志；**人绕过协议是权利，不是 bug**。

**唯一的例外处理**：当 AI 因人的催促而跳过流程时，**应当留痕**（写入决策日志或事件：如 `promote 由人直接指定，未做完整剪枝`）。这样事后可审计"这一步是走过流程还是被催过去的"——**记录不等于阻止**，这正是 Athena 一贯的"留痕而非门槛"立场（§5.1e）在责任边界上的延伸。

> 一句话：**Athena 管 AI 的默认纪律，不管人的显式意志；管不到就如实记录，不假装能拦。**

---

> 本文件当前在池中。
> 本轮已纳入：剪枝第 2 条升级为「原理实机验证」并强制**反证实验**（§5.1a–5.1d，含 fidus Niri 空值/`(0,0)` 教训）、第 4 条「成本对账」、术语热加载接口、模块化 workspace 架构。
> **关键自指**：§11 对本提案自身执行了反证实验——发现「不可关闭」在 CI/无显示环境存在失效边界，故保留 `--skip-falsification=<具象理由>` 逃生口（理由留痕 + 标红），并明确纯文档类提案改用"反例检索"。
>
> **术语约定（全文档统一）**：本协议自称"工作协议（Working Protocol）"，不称"角色卡"（避免与完整人格设定混淆）；`~/.Athena/` 称"状态库 / 协议存档"，其文档即记忆、就地修订不回滚；项目根的 `AGENTS.md` 等称"协议入口文件"；`athena` CLI 称"协议的执行边界"；**一个工作项 = 一个文件，剪枝记录是文件内章节而非独立文件**（§13.1，旧称 `<slug>.prune.md` 作废）；并发靠 **slug 级文件锁 + content-hash CAS**（§13）。术语映射见 §1.3。
> **本轮（v3）修订要点**：① 允许 AI 直接读写 `~/.Athena/`，CLI 只守位置不守权限（§1.1，与 fidus 一致）；② 反证实验由硬门槛**降级为留痕**（§5.1e，`validate` 标红而非拒绝，`--skip-falsification` 生成待测项 §5.1f）；③ `promote <slug>` 不带 from/to，**状态由名称唯一性 + 目录位置识别**（§5.2a）；④ `terms.local.{toml,md}` 声明**避免手动修改**（§2.1b）；⑤ 协议入口文件增加**显式更新路径** `init --sync`（§1.1，复制即断开引用，需三路合并）；⑥ 新增 §12「为什么不只是一组 shell 脚本」——区分骨架（shell 可替）与协议约束力（shell 无解），给出最小实现判据。
>
> **本轮（v4）修订要点**：⑦ 快速通道明确为**小修复与配置更改**（§5.4），不再泛称"小修改"；⑧ 差异化定位：OpenSpec =「规范评审流程」、Athena =「AI 纪律协议」，二者互补、Athena 可导出给 OpenSpec（§8.1）；⑨ **解析校验**：人工编辑可能让工具无法解析，操作前校验、报错交人修、绝不自动改文件（§9.1）；⑩ **多会话并发**（§13）——**剪枝记录内联**为前置条件（§13.1 + 反证实验 5），四层并发模型（文件锁 / content-hash CAS / session-id / 事件日志）、短锁为主长锁仅提示（§13.3）、多项目各自独立（§13.4）；⑪ **跨机 = 快照传承 + 本地化清洗**（§13.5）——**没有多机一致性概念**，整个 `~/.Athena` 一次性复制后 `athena localize`，之后两机完全独立；git 角色收敛为**审计层 + 传承载体**（含 `.git/` 整体复制），**不做持续同步**；连带修改 §1.2、§2.1 模板、§4、§5.1e、§5.4、§6、§9.1、§10、§12.4、§13.6、AGENTS.md ②③⑥。
>
> **本轮（v5）修订要点**：⑫ **协议入口文件膨胀控制与事后审计**（§1.1）——常驻上下文必须短，`athena agents check/diff/history` 管大小与漂移；⑬ **修正"不回滚"→ 允许 AI 压缩**（§1.3）——不回滚 ≠ 只增不减，滚雪球会让协议失效，`athena compact` 折叠过程、保留结论与 git 指针，`pending` 不得被压掉；⑭ **容器/VM 锁失效场景**（§13.2）——同一 `~/.Athena` 经 bind mount 共享时 `flock` 在 NFS/9p/virtiofs/overlay 上可能失效，做文件系统检测+警告+`O_EXCL`+TTL 兜底，**跨边界不承诺强一致**；⑮ **跨机允许 AI 同步"有必要同步的坑"**（§13.5）——坑是跨环境通用资产，AI 挑、人确认、单向点对点，绝不演成双向同步；⑯ **责任边界**（§13.8）——用户催促/直接给结论/自行绕过/模型能力/跨边界并发/团队治理，**本质不是设计问题，Athena 明确不负责**；仅要求"被催促跳过时留痕"。
>
> **本轮（v6）修订要点**：⑰ **Sys 优先的分层加载架构**（§1.1、§12.3）——Sys（System Prompt 的简写）是会话启动即注入的最靠前系统提示，**比项目入口更早更可靠**；加载优先级改为 **Sys > 用户/全局规则 > 项目入口（AGENTS.md）> 按需 context**；明确 **AGENTS.md 是"项目接口"、不是"第一道锁"**，只放项目级薄层，详情按需拉取；新增 `athena sys-export [--format claude|cursor|generic]` 生成供用户塞进 Sys 的短摘要；Sys 负责"记得加载"、context 负责"加载什么"；长会话稀释靠"Sys 极短 + hook 在动作前重跑 context/validate"，确定性拦截交给 hook 而非提示词；反证实验 4 补充"Sys 注入最可靠 / 长会话是否稀释"两组验证。
>
> **本轮（v7）修订要点**：⑱ **CLI 防线只对「愿意走 CLI 的 AI」有效**（§12.0）——因 §1.1 允许 AI 直接编辑，绕过 CLI 时锁/CAS/白名单/校验/事件日志**全部失效且不被实时阻止**；防线排序改为**协议文本（最强）> CLI 边界（只对愿意者）> 事后校验+审计**；**Athena 是工作协议，不是权限系统**；⑲ **并发模型自我质疑为最典型的过度设计**（§13 开头）——真实频率极低、且只在愿意走 CLI 时生效；给出四级降级路径，**内联（§13.1）必做、并发四层是预案按需**，不让预案变成预支的复杂度；⑳ **复杂度对人的成本是 0**（§1.3）——术语/剪枝/反证/成本对账/压缩/localize 全甩 AI，人只做"说想法 + 点头/摇头"；这也是必须允许 AI 直接编辑的原因（否则复杂度滑回人）；㉑ **第 4 条成本对账的本质是迫使 AI 显式写出比较**（§5.1）——不是要求算准数字，而是打破"我们已接近成功"这句免费修辞，校验只看表填没填、拒绝理由具象不具象；㉒ **Phase 3 取消 dogfood 判定**（§10）——Athena 本身就是我的工作流（照抄 fidus），不是待验证假设；改为 **Migration + 边用边改**，判定标准换成"用起来顺不顺手"（想不想得起来 context / 剪枝是不是想跳过 / 术语够不够用 / localize·compact 是不是从没用过 → 是则删）；㉓ **不设防：加密与访问控制都不做**（§1.3、§1.1b）——技术上可直接加密但没必要，**因为 fidus 从不靠保密**；加密会破坏 AI 直读写、纯文本审计、跨机传承，并让复杂度滑回人身上。由此提炼贯穿原则：**Athena 不阻止，只记录与提示**（反证=留痕、校验=报错不代改、CLI=只对愿意者、锁=预案、边界=照做只留痕、防读取=不做）。今后任何"防住什么"的提议，先问：**fidus 需要过吗？** 答案基本是没有。
>
> **本轮（v8）· 原理级重写**：全文口径此前一直停留在**防守姿态**（把 CLI 当门禁/防线、把 AI 直编辑当"被允许的特例"、把 validate 当"拒绝"）——这与核心原理冲突。现按 **§1.3 一句话定义**重新校正：**Athena = 以提示词工程为原理 + AI 驱动的状态机**。两个关键词的推论：① **提示词工程 → 靠说服不靠强制**，改用拦截维持流程就不再是提示词工程；② **AI 驱动 → AI 是主体**，因此**必须能直接读写 `~/.Athena/`**（阻止它与原理直接冲突，不是恩赐是要求）。据此重写：§1.1（直读写是原理的推论）、§1.3（新增原理定义 + 口径修正对照表）、§2.1（模板是强提示非拦截）、§2.3/§3（canonicalize 是防呆、write 是顺手工具非唯一入口）、§5.1a（反证=留痕）、§9.1（validate 是**给 AI 的自检报告**，编辑者通常是 AI 而非人）、§12.0（**CLI 是脚手架，不是镣铐**，附机制真实作用对照表）；AGENTS.md 头部、④、168/445/479 行同步。**判断句式：若哪天要靠"拦住 AI"才能维持流程，说明协议文本没写好——去改协议，不是加固 CLI。**
>
> **本轮（v9）· 状态语义双轴化（重大结构简化）**：此前"提案/草案/方案"与"池/进行中/…"被混在同一条轴上（表现为 `working/draft/`、`working/plan/` 嵌套目录），导致"文档是什么"与"推到哪一步"纠缠。现按 fidus 原始语义拆为**两条正交的轴**：**轴一 文档类型**（提案=我有个想法 / 草案=我打个草稿这东西怎么实现 / 方案=我可以按照这个方案实施）——由 `kind` 字段 + 内容表达，**原地深化，文件不动**；**轴二 推进状态**（池=未推进的提案 / 进行中=我正在推进的提案·草案·方案 / 结束·冻结=已完成·已毙 / 社区互助=字面意思放出去请人帮忙）——**由目录表达**。目录从 8 个精简为 4 个：`pool/ working/ finished/ community/`（不再有 `working/draft`、`working/plan`、`done`、`frozen`、`suspended`、`archives`；完成与毙掉同在 `finished/` 用 `outcome` 字段区分，归档即留在 `finished/`）。配套新增 `athena deepen <slug> --to draft|plan`（轴一，改 kind 不挪文件）与 `athena community <slug>`（社区互助出口，供**人**搬运、不做机器同步）；`promote` 语义收敛为纯轴二推进。同步修改 §0、§1 目录树、§2.1 frontmatter（新增 `kind` / `outcome`，`status` 降级为目录的冗余快照且**冲突时以目录为真值**）、§5 剪枝、§5.2 转换表、§6 命令集、§7 context 输出、§12 shell 示例；AGENTS.md ③ 新增双轴表。**一句话：目录回答"推到哪一步"，文档自己回答"它是什么"。**
>
> **评审时最可能的质疑与答复**：「这套东西一个 bash 函数就能写」。答复见 §12——bash 能写骨架与 mv，但守不住 ① 转换旁路、② 结构化留痕、③ 路径越界、④ 术语驱动校验，且完全无解 ⑤ 让 AI 读到并遵守协议。核心资产是协议文本，CLI 只是最小执行边界。
> 待剪枝项：`pending` 待测项的自动提醒机制（context 是否默认展示 pending 列表）；`[term.falsification].mode` 项目级切换的 UX；`--sync` 冲突段的手动解决引导；**本协议入口文件（AGENTS.md）自身已膨胀至 ~480 行，远超 §1.1 建议的 150 行阈值**——须按该节策略处理：把"不用的后果""完整规则""术语映射"外置为按需拉取（`athena context <section>`），入口只留协议摘要 + 命令索引 + 位置边界。这是**自指问题，优先处理**（v5 已识别，待 v6 执行）。
> 经完整剪枝与**原理实机验证（含反证留痕）**后，由 `athena promote` 移入 `working/`（进行中）。
